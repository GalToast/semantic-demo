# W20 Prompt Context — Status as of 2026-06-17

**RECONCILED 2026-06-17** (origin/master HEAD: `2feb630`)

This file is the dispatch prompt context for W20 work. It captures the
current state so future subagents and main-lane runs have accurate ground
truth without re-reading the full git log.

---

## What's Done (W19 + W20 cumulative)

### Lane A + B rewires (`@lib/*` canonical migration)

- `b13cfc2` Lane A: `src/lib/engine/lifecycle-bridge.ts` → `@lib/orchestration/lifecycle`
- `7c99ff9` Lane B: 3 files (`cursor.ts`, `demo-choreography.ts`, `scene-reveal.ts`) rewired to canonicals
- `9b585df` Parallel session: repointed orphaned `js/` imports to `src/lib/` canonicals

### Cycle break + investigation

- `6f396e6` Break `lifecycle.ts ↔ journey.ts` circular dependency (split imports into `@lib/journey/selected-card` + `@lib/journey/thread-settler` + `@lib/stores/lifecycle`)
- `d4662b8` Cross-import map (`docs/w20-cross-import-map.md`) — 14 edges identified
- `4d108a4` Open-questions investigation (`docs/w20-open-questions-investigation.md`) — 5 verdicts
- `79ac0e0` Regression test locking Lane A canonical
- `bfcc1cf` Regression test for Q2 (`setSemanticDiveMode` 3-layer chain)

### Wave 3 — internal js/modules/ cross-import rewiring

- `11e4c68` Wave 3 Step 1-3: deleted 2 zero-inbound dead files (`app-svelte-island.ts`, `three-node-manager.ts`)
- `7b67cfc` + `5f69f27` Wave 3 Step 4+5: composition-state canonical created + 3 importers rewired + lifecycle/loading-ui call sites fixed
- `15e04aa` Wave 3 Step 7 partial: 1 of 7 lifecycle.ts relative imports rewired (view-controller)
- `57645cd` Wave 3 Step 6: rewired journey.ts inbound importers to `@lib/journey/*` canonicals (4 files: journey-compass-controller, journey-semantic-overlay, lifecycle-reset, view-controller)
- `6a89f9b` Wave 3 Step 7: completed lifecycle.ts rewires (5 remaining imports). After this commit, lifecycle.ts has zero remaining `./` relative imports

### Wave 4 prep — deep-relative path cleanup

- `2feb630` Wave 4 prep: rewired 5 `../../src/lib/...` deep-relative imports to `@lib/*` bare specifiers (event-bindings, journey-thread-settler, navigation-state, thread-inspector, view-controller)

### Regression tests

- `bc650c3` composition-state canonical regression test (6/6 PASS)
- `bfcc1cf` `setSemanticDiveMode` 3-layer canonical chain test (6/6 PASS)
- `79ac0e0` lifecycle-bridge canonical regression test
- `99cb0f6` store-parity-mirror regression test (GAP-4 + GAP-5)

### Test infrastructure

- `a464cc1` Parallel session: repointed 5 test reads from `js/modules/` to `src/lib/` or `legacy-reference/`
- `505ad77` Parallel session: integration test for W15 body-attr live probe
- `693b500` Exempted `ba6ad56` parallel commit from commit-purity-invariant

### Parallel session work (W19/W20 boundary)

- `7c131d7` fix(stores): force-write parity attributes on every `refreshCompositionState` (W15 parity-attrs gap)
- `2b100a1` docs(notes): preserve W15 parity-attrs diagnostic report

---

## What's Still Pending (W20)

### Wave 4 — final file deletions

**5 files already deleted** in W19+W20 (loading-ui.ts, composition-state.ts,
exploration-mode.ts by parallel session; app-svelte-island.ts, three-node-manager.ts
in `11e4c68`). The parallel session has 30 files with `M` status (modified) in
`js/modules/` — these are in-flight WIP, not deletion candidates.

**Remaining relative imports in lifecycle.ts** (2 deep-relative, post-Wave 3 Step 7):

1. `../../src/lib/journey/thread-settler-adapter` (line 13) — `traverseNeighbor`
2. `../../src/lib/journey/semantic-guide.ts` (line 18) — `hideSummaryCard as hideSummaryCardImpl`

**Remaining relative imports in journey.ts** (1 deep-relative):

1. `../../src/lib/journey/thread-settler-adapter` (line 40) — `traverseNeighbor`, `previewInsideNextThread`

**Other deep-relative paths remaining** (in parallel-session WIP files):

- `journey-compass-controller.ts` line 22: `../../src/lib/journey/semantic-dive.ts`
- `js/modules/components/InfoPanelChrome.svelte` line 3: `../../../src/lib/engine/demo-choreography.ts`
- `js/modules/utils/dom-builder.ts` lines 8-9: re-exports from `../../../src/lib/utils/dom-builder`
- `js/modules/utils/focus-trap.ts` line 8: re-exports from `../../../src/lib/utils/focus-trap`
- `js/modules/utils/three-textures.ts` line 12: re-exports from `../../../src/lib/utils/three-textures`

After these are cleaned up, `js/modules/journey.ts` and `js/modules/lifecycle.ts`
can be deleted (they are now thin re-export shims).

### Deferred items (parallel session WIP, D-staged)

- `loading-ui.ts` — deleted by parallel session (no longer on disk)
- `composition-state.ts` — deleted by parallel session (no longer on disk)
- `exploration-mode.ts` — deleted by parallel session (no longer on disk)

---

## Current File Counts

| Category | Count | Notes |
|---|---|---|
| `js/modules/` files on disk | 62 | Includes `components/`, `utils/`, `view-models/` subdirs |
| Files deleted in W19+W20 | 5 | app-svelte-island, three-node-manager, loading-ui, composition-state, exploration-mode |
| Parallel-session `M` files | 30 | In-flight WIP, do not touch |
| Regression tests | 5 | In `tests/unit-active/*regression*` |
| Deep-relative paths remaining | 8 | Across 6 files (lifecycle, journey, compass-controller, InfoPanelChrome, utils/*) |

---

## Critical Files to Read First

When resuming W20 work, read these in order:

1. `docs/w20-cross-import-map.md` — 14-edge cross-import map with circular-dep + canonical homes
2. `docs/w20-open-questions-investigation.md` — Q1-Q5 verdicts + Wave 3 recommendations
3. `docs/w19-charter-2026-06-17.md` — Wave 1-4 plan with blocker table
4. `notes/w19-lane-a-b-handoff-2026-06-17.md` — W19 lessons (especially lesson 7 about kilo/openrouter/owl-alpha route)

## Important Operating Rules

- **Subagent model**: Use `openrouter/owl-alpha` (NOT `kilo/openrouter/owl-alpha`) — the kilo gateway is timing out. OpenRouter direct is the recommended route.
- **Always use `git commit --only <pathspec>`** to avoid pulling in parallel session WIP
- **Check `git status --short` before committing** — files marked `M` are owned by the parallel session
- **Multi-line `export { ... }` blocks hide exports from `rg "^export.*\bfn\b"`** — always verify with `rg -B 5 "\bfn\b" file`
- **DO NOT use `pi_background_jobs action: "poll"`** — that action is invalid (use `list|tail|kill|clear` or skip)
- **When subagent hits rate limit**, take over in main lane for small fixes (test assertions, simple reverts)
- **Check `docs/w20-cross-import-map.md` BEFORE trusting it** — Owl-2 found it was wrong about Q1 (loading-ui), Q3 (derivePanelSurface), Q5 (setSemanticDiveMode). Always verify canonical locations by `rg "^export"` rather than trusting the map

## Subagent Friction Pattern (Lessons Learned)

- **Worker stuck after first bash call** → model is too large or rate-limited. Cancel + re-dispatch with smaller model.
- **Worker committed only 1 of 5 expected files** → verify commit with `git show --stat <sha>`, not the commit message.
- **Worker hit `pi_background_jobs action: "poll"` validation error** → keeps retrying the invalid call, burns 5+ minutes. Steer with explicit "do NOT call pi_background_jobs" instruction.
- **Worker finds signature mismatch** → rewire at call sites too (not just imports). Common pattern: legacy `fn(arg)` vs canonical `fn()`.
- **Parallel session force-pushes** → can wipe local commits. Recover with `git cherry-pick <lost-sha>` from reflog.

---

## Reconciliation Log

| Round | Commit | What changed |
|---|---|---|
| 1 (original) | `75f04ba` | Created file with ground truth at `bc650c3`. Claimed 11 commits landed, Wave 3 Step 6 + 7 + Wave 4 pending. |
| 2 (this file) | — | Reconciled to `2feb630`. Updated: HEAD SHA, added 8 new commits to "What's Done" (6a89f9b, 57645cd, 2feb630, bc650c3, 15e04aa, 7c131d7, 2b100a1, 505ad77), marked Wave 3 Steps 6+7 as DONE, added Wave 4 prep section, corrected file counts (62 on disk, 5 deleted, 30 parallel WIP), added remaining deep-relative import inventory, added file-count table, added this reconciliation log. |
