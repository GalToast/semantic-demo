# Active Unit Tests

This directory contains current unit tests for the canonical Svelte/TypeScript
track under `src/`.

`npm run test:unit` runs only this directory. Preserve old assertions in
`tests/unit/` until they are converted here or are no longer useful as reference.
