# W13-T5b Charter — Final state migration (2026-06-16)

> **Goal:** Delete `js/state.ts` (43,564 bytes) and `js/state/selectors/index.ts` (14 LOC) once all 65 consumers are migrated to use the Svelte 5 state class at `src/lib/state/app.svelte.ts` and the updated bridges.
> **Status:** W13-T5a partially migrated 27 consumers (commit `41b2d09`). The remaining ~38 files still import from `js/state.ts` and 10 from `js/state/selectors/index.ts`.
> **Blocker:** Deletion without migration causes 150 svelte-check errors across ~80 files.

## Scope (7 waves, ~14-20 hours of work)

### Current state
- `js/state.ts` (43,564 bytes) — legacy state with 28+ exported types and values
- `js/state/selectors/index.ts` (14 LOC) — barrel re-exporting 9 .js shims
- `src/lib/engine/state-bridge.ts` — re-exports `state` + types from `js/state`
- `src/lib/engine/state-selectors-bridge.ts` — re-exports from `js/state/selectors/index`
- 65 files import from `js/state.ts`
- 10 files import from `js/state/selectors/index.ts` (mostly overlap)
- 68 total import statements

### Canonical target
- `src/lib/state/app.svelte.ts` — Svelte 5 state class (W11-T1)
- `src/lib/engine/state-bridge.ts` — needs to be REPOINTED to `app.svelte.ts` (currently re-exports from `js/state`)
- `src/lib/engine/state-selectors-bridge.ts` — needs to be REPOINTED similarly

### Wave 1: Bridge repointing + surface audit (1-2 hours)

**Files to modify:**
1. `src/lib/engine/state-bridge.ts` — repoint to `app.svelte.ts`, add type re-exports
2. `src/lib/engine/state-selectors-bridge.ts` — repoint to `app.svelte.ts` (or a new `selectors.ts` module)

**Verification:**
- Run `svelte-check` to identify which types/values from `js/state.ts` are NOT in `app.svelte.ts`
- Build a migration map: which consumers need type-only imports vs value imports

**Outcome:** Bridges now point to canonical. The "is the Svelte 5 class a complete superset?" question is answered.

### Wave 2: Kernel consumer migration (1-2 hours)

**Files (8-10 simple kernel files):**
- `js/modules/utils/geo-data.ts`
- `js/modules/utils/seeded-random.ts`
- `js/modules/cluster-filter.ts`
- `js/modules/cluster-ui-accent.ts`
- `js/modules/filter-state.ts`
- `js/modules/composition-state.ts`
- `js/modules/exploration-mode.ts`
- `js/modules/audio-scape.ts`
- `js/modules/connection-analysis-adapter.ts`
- `js/modules/event-bindings.ts`

**Approach:** Each file rewires its `import { ... } from '../state'` to `import { ... } from '@lib/engine/state-bridge'`. Type-only imports use `import type { ... }` for clarity.

### Wave 3: Bindings consumer migration (1-2 hours)

**Files (8-10 binding files):**
- `js/modules/bindings/global-bindings.ts`
- `js/modules/bindings/journey-bindings.ts`
- `js/modules/bindings/legend-bindings.ts`
- `js/modules/bindings/mode-bindings.ts`
- `js/modules/bindings/onboarding-bindings.ts`
- `js/modules/bindings/suggestion-bindings.ts`
- `js/modules/bindings/view-bindings.ts`
- `js/modules/bindings/keyboard-help.ts`
- `js/modules/bindings/filter-bindings.ts`

**Approach:** Same as Wave 2, but bindings often have more complex type dependencies. May need to add new exports to `app.svelte.ts` for state-machine types.

### Wave 4: Journey subsystem consumer migration (2-3 hours)

**Files (10-12 journey files):**
- `js/modules/journey-arrival-handoff.ts`
- `js/modules/journey-bindings.ts`
- `js/modules/journey-canvas-hover.ts`
- `js/modules/journey-canvas-interaction.ts`
- `js/modules/journey-canvas-node-picking.ts`
- `js/modules/journey-canvas-hit-test.ts`
- `js/modules/journey-choreography-types.ts`
- `js/modules/journey-compass-controller.ts`
- `js/modules/journey-compass-state.ts`
- `js/modules/journey-focus-ui.ts`
- `js/modules/journey-neighborhood.ts`
- `js/modules/journey-point-color.ts`
- `js/modules/journey-route-trace.ts`
- `js/modules/journey-selected-card.ts`
- `js/modules/journey-semantic-overlay.ts`
- `js/modules/journey-text-helpers.ts`
- `js/modules/journey-thread-model.ts`
- `js/modules/journey-thread-settler.ts`
- `js/modules/journey-webgl-utils.ts`
- `js/modules/journey-webgl.ts`

**Approach:** Journey subsystem is the largest consumer. Many of these already have Svelte 5 ports in `src/lib/journey/` — they should import from there, not from `state-bridge`.

### Wave 5: Three.js + remaining js/modules/ files (2-3 hours)

**Files (10+ files):**
- `js/modules/three-engine.ts`
- `js/modules/three-interaction-visuals.ts`
- `js/modules/three-node-manager.ts`
- `js/modules/three-postprocessing.ts`
- `js/modules/three-search-animations.ts`
- `js/modules/three-thread-manager.ts`
- `js/modules/tooltip.ts`
- `js/modules/weather.ts`
- `js/modules/view-controller.ts`
- `js/modules/stores.ts`
- `js/modules/strand-continuity.ts`
- `js/modules/focus-anchor-indicator.ts`
- `js/modules/focus-pocket-geometry.ts`
- `js/modules/focus-pocket-personality.ts`
- `js/modules/lifecycle.ts`
- `js/modules/lifecycle-reset.ts`
- `js/modules/lifecycle-search-sync.ts`
- `js/modules/url-state.ts`
- `js/modules/thread-inspector.ts`
- `js/modules/legend-ui.ts`
- `js/modules/app.ts`

**Approach:** These have varying complexity. Some are heavy kernel files (three-engine, lifecycle), some are simple adapters. May require bridge surface expansion.

### Wave 6: src/ consumer migration (1-2 hours)

**Files (src/ files that import from js/state):**
- `src/lib/engine/state-selectors-bridge.ts` (already migrated in Wave 1)
- `src/lib/search/legacy-exports.ts`
- `src/lib/journey/canvas-interaction.ts` (if exists)
- `src/lib/journey/focus-ui.ts`
- `src/lib/journey/thread-inspector.ts`
- `src/lib/journey/legend-ui.ts` (if exists)
- `src/lib/orchestration/window-actions.ts` (if exists)

**Approach:** These should mostly import from canonical `app.svelte.ts` directly, not via the bridge.

### Wave 7: Final cleanup + state/selectors deletion (30 min)

**Steps:**
1. Verify all 65+ consumers are migrated
2. Run `git grep -l "from.*['\"].*\.\./state(\.ts)?['\"]" -- ':!*.lock' ':!node_modules' ':!legacy-reference'` — should return 0 results
3. Delete `js/state.ts` and `js/state/selectors/index.ts`
4. Update `src/lib/engine/state-bridge.ts` to be a re-export from `app.svelte.ts` (already done in Wave 1)
5. Update `src/lib/engine/state-selectors-bridge.ts` to be a re-export (already done in Wave 1)
6. Commit as `chore(w13-t5b): delete legacy js/state.ts + js/state/selectors/index.ts — final state migration`
7. Verify: svelte-check 0 errors, vitest 652/652

## Time budget per wave

- Wave 1: 1-2 hours
- Wave 2-3: 1-2 hours each (mostly mechanical rewires)
- Wave 4-5: 2-3 hours each (more complex type dependencies)
- Wave 6: 1-2 hours
- Wave 7: 30 min

**Total: ~10-15 hours of focused work, spread across 1-3 sessions.**

## Subagent dispatch pattern

Each wave (1-6) can be dispatched as a single worker with a clear scope. Pattern from W11/W16:
1. Worker reads the charter
2. Worker updates the assigned N files (only those)
3. Worker runs svelte-check + vitest to verify
4. Worker commits with the wave ID
5. Main lane reviews + pushes

For Wave 7 (final), the worker should:
1. Verify all migrations complete
2. Delete the 2 files
3. Commit + push
4. Report any consumers still using legacy paths

## Verification protocol (each wave)

```bash
# 1. svelte-check: 0 new errors
npx svelte-check --threshold error 2>&1 | tail -3

# 2. test:unit: 652/652 must still pass
npx vitest run 2>&1 | tail -5

# 3. bridge contract: 5/5 must pass
npx vitest run tests/unit-active/svelte-bridge-import-contract.test.ts 2>&1 | tail -5

# 4. ts-js-drift: clean
node tests/ts-js-drift-contract.mjs 2>&1 | tail -3

# 5. diff stat: only the N assigned files
git status --short | head -10
echo "Expected: ~N files (only the wave's scope)"
```

## Open questions

1. **Does the Svelte 5 class have all 28+ exports from `js/state.ts`?** Wave 1 will surface this. If gaps exist, the Svelte 5 class needs extension first.
2. **What about the 9 BOTH-pattern .js shims in `js/state/selectors/`?** They're re-exported through `index.ts`. After Wave 1's bridge repointing, they're dead code. Can be deleted in a sub-wave before Wave 7.
3. **Should the 2 bridges (state-bridge, state-selectors-bridge) be deleted too?** They're thin re-exports. The canonical target is `app.svelte.ts`. After Wave 1, consumers could import directly from `app.svelte.ts`. The bridges can be deleted in a future wave.

## Risk assessment

- **HIGH**: Wave 1 (bridge repointing) — risk of breaking all 65+ consumers if the Svelte 5 class doesn't have all the exports
- **MEDIUM**: Waves 2-3 (consumer migration) — mechanical rewires, low risk if bridge surface is complete
- **HIGH**: Wave 4-5 (journey + three.js consumers) — complex type dependencies, may need new exports
- **LOW**: Wave 6 (src/ consumers) — same as Waves 2-3 but for src/ files
- **MEDIUM**: Wave 7 (final deletion) — if any consumer is missed, the deletion breaks the build

## Success criteria

- All 65+ consumers migrated to canonical Svelte 5 class
- `js/state.ts` and `js/state/selectors/index.ts` deleted
- `src/lib/state/app.svelte.ts` is the only state source
- svelte-check 0 errors, vitest 652/652, bridge 5/5
- `state-bridge` and `state-selectors-bridge` either deleted or simplified to a re-export
- Memory consolidation: the W13 charter can be marked "complete"
