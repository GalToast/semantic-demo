# Wave W8 Charter: Bridge Retirement & Canvas Component Lifecycle Ownership

**Date:** 2026-06-20
**Scope:** Phase 6A & Phase 6B cleanup: Remove unused legacy Engine Bridge, type definitions, and adapters. Move types to self-contained engine declarations and point Svelte Canvas directly to the high-performance local engine modules.

## Goals

1. **Delete Dead Bridge Code**: Fully eliminate the unused `src/lib/engine/adapters/core.ts` file (which holds `createEngineBridge`) and the old adapters folder.
2. **Slim Component Imports**: Update `src/components/Canvas.svelte` to import `EngineCallbacks` directly from `@lib/engine/lifecycle` instead of the legacy `@lib/engine/adapters/types.ts` type-bridge.
3. **Streamline Barrel Exports**: Clean up `@lib/engine` barrel `index.ts` to stop re-exporting defunct bridge factories and legacy interfaces.
4. **Preserve Integrity**: Ensure 100% test compatibility against all active contract and unit assertions with zero regressions.

## Pipeline Tasks

### Task 1: Redirect Canvas Engine Type references

- In `src/components/Canvas.svelte`, import `EngineCallbacks` directly from `@lib/engine/lifecycle`.
- Double check that `src/lib/engine/lifecycle.ts` has a fully compliant `EngineCallbacks` signature.

### Task 2: Remove the `adapters` folder

- Delete `src/lib/engine/adapters/core.ts` (Composition root).
- Delete `src/lib/engine/adapters/types.ts` (Defunct bridge types).
- Delete `src/lib/engine/adapters/lifecycle-bridge.ts` (Defunct legacy lifecycle bridge methods).

### Task 3: Simplify `src/lib/engine/index.ts`

- Remove `export { createEngineBridge } from './adapters/core'`.
- Remove the exports of type definitions from `./adapters/types`.
- Keep direct TS re-exports intact to support downstream callers.

## Verification Checklist

- [ ] **Tests pass**: `npm run test` (1,142/1,142 green, including contract checks).
- [ ] **TypeScript compiles**: `npm run typecheck` produces 0 errors.
- [ ] **Svelte build works**: `npm run build:svelte` output bundles clean, with a lean entry-point structure.
