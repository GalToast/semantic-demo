# W16 Charter — Remaining Legacy Bridges & Visual QA Closeout

**Date:** 2026-06-16
**Scope:** Complete the final ~5 legacy bridge retirements, unblock `event-bindings`, close Visual QA

---

## What's left (down from 22 bridges to 5)

| # | File | LOC | Status | Approach |
|---|---|---|---|---|
| 1 | `three-interaction-visuals` | 680 | 🔄 In flight (subagent) | Three.js pair with `three-search-animations` |
| 2 | `three-search-animations` | 525 | 🔄 In flight (subagent) | Three.js pair with `three-interaction-visuals` |
| 3 | `lifecycle` | 250 | 📋 Recon dispatched (subagent) | 20 consumers — use recon report to sequence |
| 4 | `event-bindings` bindings | n/a | 🔴 Blocked | Port the 12 `js/modules/bindings/*` files it imports |
| 5 | `semantic-lane` | 503 | ✅ Done (parallel session) | Already flipped — verify and close |

---

## Wave I: Three.js Block (680 + 525 LOC)

**Already dispatched:** subagent `ocw_b4aab05b` handling both files as a unit.

**Dependencies to verify:**

- `three-interaction-visuals` imports `three-search-animations` — must point to canonical after port
- Both import `THREE` from `three` and `webglContext` from `@lib/engine/webgl-context`
- `three-interaction-visuals` also imports `focus-anchor-indicator` from `@lib/journey/focus-anchor-indicator` (already canonical)

**Quality gates:**

- `grep "from.*js/modules" src/lib/engine/three-*.ts` → 0 matches
- `svelte-check` → 0 errors
- `npm run test:unit` → all pass

---

## Wave J: Lifecycle Port (250 LOC, 20 consumers)

**Recon dispatched:** subagent `ocw_daefdecf` mapping consumer readiness.

**Expected outcomes from recon:**

- How many of the 20 consumers are already canonical?
- Which consumers are still legacy-only?
- Can `lifecycle` be safely ported, or is it blocked by heavy legacy dependency chains?

**After recon:**

1. If ≥15 consumers are canonical → dispatch port subagent immediately
2. If <15 → sequence: port canonical consumers first, then lifecycle
3. If blocked by unported deps → file as W17

---

## Wave K: Event-Bindings Unblock

`src/lib/ui/event-bindings.ts` is already canonical but still imports from 12 `js/modules/bindings/` files.

**Two paths:**

- **A.** Port each of the 12 bindings to `src/lib/ui/bindings/*.ts`
- **B.** Convert `event-bindings.ts` to import through existing bridges where possible, then retire

**Recommendation:** Do A only if bindings have consumers outside `event-bindings`. If they're single-consumer (used only by `event-bindings`), inline them into `event-bindings.ts` and delete.

---

## Track 1: Visual QA Closeout

**Blocked** on PHP server for live data. Two paths:

1. Start `php -S 127.0.0.1:8795` and retry with loaded app
2. Verify the 14:54 Round 3 screenshots are sufficient evidence

**Recommendation:** If the PHP server works, do a single-headed Playwright pass for the 4 deferred states. If not, accept Round 3 evidence and close Track 1.

---

## Deliverables

| Wave | Target | Deliverable |
|---|---|---|
| I | Three.js | Commit 2 ports + 2 bridge flips |
| J | Lifecycle | Recon report → port if ready |
| K | Event-bindings | Unblock plan from recon |
| Track 1 | Visual QA | Either live run or accept Round 3 evidence |

**End-state for W16:** All 22 legacy bridges flipped to canonical. W17 is clean-up (delete death-bridges, W17 charter, etc.).
