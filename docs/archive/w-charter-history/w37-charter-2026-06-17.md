# W37 Charter — Ship Verification: Deploy Scripts + CI + Test Cleanup

**Date:** 2026-06-17
**Status:** Migration structurally complete. 21 tests reference deleted journey.ts. Deploy scripts untested. CI unverified.
**Rule:** Do NOT touch parallel-session modified files.

---

## 🎯 W37 Goals

### Goal 1: Deploy Script Verification

Test that deploy.sh and deploy.ps1 still work after deleting js/modules/.

**Tasks:**

1. Read deploy.sh and deploy.ps1
2. Check for references to js/modules/ or deleted files
3. Run a dry-run or local test deploy
4. Fix any broken paths
5. Commit fixes

### Goal 2: Broken Test Cleanup

Fix or retire the 21 tests that reference deleted js/modules/journey.ts.

**Tasks:**

1. Find all 21 test files: rg "journey\\.ts" tests/ -l
2. For each test, determine if it has a canonical equivalent in src/lib/
3. If yes: repoint the import
4. If no: retire the test (move to tests/retired/ or delete)
5. Run tests to verify
6. Commit

### Goal 3: CI Pipeline Verification

Verify .github/workflows/ci.yml works with the new structure.

**Tasks:**

1. Read ci.yml
2. Check for references to deleted files, old paths, or outdated test commands
3. Update if needed:
    - Ensure svelte-check runs
    - Ensure build runs
    - Ensure test commands reference existing files
4. Commit
5. (Optional) Trigger a test run

### Goal 4: Performance Budget Document

Write formal performance targets based on measured data.

**Tasks:**

1. Write docs/performance-budget-2026-06-17.md with:
    - Bundle size targets (main chunk, three.js chunk)
    - Load time targets (FCP, LCP, TTI)
    - Lighthouse score targets
    - Measurement methodology
2. Commit

---

## 🚦 Coordination

1. Safe zones: deploy scripts, tests/, .github/workflows/, docs/
2. Do NOT touch: parallel-session modified files from git status --short

## Success Criteria

- [ ] Deploy scripts verified working (or fixed)
- [ ] 21 broken tests fixed or retired
- [ ] CI workflow verified and updated
- [ ] Performance budget written
- [ ] Build passes, svelte-check 0/0
- [ ] All committed and pushed
