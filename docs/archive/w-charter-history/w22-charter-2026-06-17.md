# Wave 22 Charter — 2026-06-17

**Author:** main lane
**Status:** charter synthesis complete, ready for execution
**Scope:** CSS ownership cleanup, Svelte a11y hardening, test consolidation, W20 retrospective application
**Estimated effort:** 5-8 subagent dispatches over 2-3 days
**Parallel session note:** Run `git status --short` before every commit; use `git commit --only` for all commits

---

## 1. Wave 21 Accomplishments

Wave 21 ran in two sub-waves plus a parallel session contribution. All planned work landed.

### W21.1 — Test Suite Hardening + CSS Quick Wins (10 commits)

1. **Readiness gate restructured** (`10bd76a`): `w20-wave4-readiness-regression.test.ts` restructured as a status report — 22 → 14 tests, clear "PENDING WAVE 4 CLEANUP" messages. Suite failures reduced from 9 to 3+.
2. **State-class-migration consolidation** (7 commits, `a6b1bb1` → `a0d31fa`): 10 of 12 state-class-migration tests consolidated into parameterized files. Compass, camera, legend, filter, search, engine-bridge, weather merged into main scaffold; viewport, focus, demo migrated to separate consolidated files. 2 remain standalone: navigation and journey (unique T4-template pattern with `delete` on mockState).
3. **Orchestrator fixes** (`42aa09b`, `0e85e42`, `5a2e362`): Stopped mirroring legacy mode/surface in `syncSvelteNavFromLegacy`, rewired lifecycle + journey-compass-controller deep-relative imports to `@lib/*` canonicals.
4. **Bridge port** (`5f6c3d9`): Tooltip ported from `js/modules/tooltip.ts` to `@lib/ui/tooltip`, legacy file deleted.

### W21.2 — Svelte Hardening (1 commit, parallel session contribution)

5. **A11y + error + loading states** (`14a9852`): Parallel session landed accessibility improvements across 5 components — FocusCard, InfoPanel, JourneyChrome, Filters, SearchInput. Added `role` attributes, loading skeletons, keyboard handlers, and ARIA properties. This was work we were about to dispatch, saving a full subagent cycle.

### W21.3 — Docs + Cleanup (5 commits)

6. **Three audit docs committed**: CSS ownership investigation (301L), test consolidation audit (257L), Svelte component audit (346L) — 904 lines of source material for W22.
7. **Legacy file deletion** (`7a89261`): 4 zero-consumer legacy files deleted.
8. **Focus-pocket simplification** (`6f05d03`, current HEAD): Removed `visible` prop and `window.__APP_STATE__` fallback, simplified to 3D-only DOM anchor.

### W21 Net Result

- **Commits:** 16+ across W21.1 (10), W21.2 (1 commit by parallel session), W21.3 (5)
- **Test suite:** 10 files → 3 consolidated files + 2 separate files; readiness gate restructured
- **Svelte components:** 5 components hardened (FocusCard, InfoPanel, JourneyChrome, Filters, SearchInput)
- **CSS:** No changes (audit-only — smells documented for W22)
- **Current HEAD:** `6f05d03` — `chore(focus-pocket): simplify to 3D-only DOM anchor`

---

## 2. W20 Retrospective Lessons Applied to W22

From `notes/w20-retrospective-2026-06-17.md`:

| Lesson                                    | W20 evidence                                   | W22 mitigation                                             |
| ----------------------------------------- | ---------------------------------------------- | ---------------------------------------------------------- |
| **Default to mimo-v2.5 paid**             | 5/5 success (100%) vs owl-alpha 1/6 (17%)      | All W22 dispatches use `opencode-go/mimo-v2.5`             |
| **Avoid free models for implementation**  | owl-alpha: 429 rate limits on every attempt    | Never use free models for write-heavy tasks                |
| **`git commit --only <pathspec>`**        | Parallel session had 13 M-flagged files        | Every W22 commit uses `--only`                             |
| **Verify before trust**                   | Cross-import map was wrong 3 times             | Verify orphans with `rg` before deleting CSS               |
| **Worker file size limit**                | Mimo-2.5 cannot reliably write >800 lines      | Split large file writes; use Edit for >800L changes        |
| **Avoid `pi_background_jobs` in workers** | Workers burned 5+ min retrying invalid action  | Hard rule: "DO NOT use pi_background_jobs" in every prompt |
| **Same-file race condition**              | Multiple workers editing same file = conflicts | Each worker owns separate files; no shared write scope     |
| **Subagent prompt template**              | Proven in W20, 100% success                    | Reuse exact template with file ownership boundaries        |

### Subagent Prompt Template (copy-paste for W22)

```
<TASK>Specific scoped task with file ownership boundaries</TASK>
<RULES>
1. Report exposed tools before starting work
2. Use git commit --only <pathspec> for every commit
3. Run git status --short before committing
4. Run git show --stat HEAD after committing
5. DO NOT use pi_background_jobs — use git status instead
6. If you find a bug outside your scope, STOP and report it
7. Max 800 lines per file write — use Edit tool for larger changes
</RULES>
<VERIFICATION>Exact commands to run before marking done</VERIFICATION>
```

---

## 3. W21 Audit Synthesis → W22 Work Items

### Source Audits

| #   | Audit                                                 | Lines | Key findings relevant to W22                                                                                                                                                          |
| --- | ----------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `notes/w21-css-ownership-investigation-2026-06-17.md` | 301   | 5 smells: .focus-stage tail-authority (HIGH), .info-panel 13-file scatter (HIGH), .is-active proliferation (MEDIUM), landscape breakpoints (MEDIUM), demo_ui.css orphans (LOW)        |
| 2   | `notes/w21-test-consolidation-audit-2026-06-17.md`    | 257   | Remaining opportunities: 2 standalone state-class tests (navigation, journey), 3 canonical regression tests mergeable, tests/unit/ legacy retirement                                  |
| 3   | `notes/w21-svelte-component-audit-2026-06-17.md`      | 346   | W21.2 hardened 5 components; remaining gaps: component test foundation (0 tests for 26 components), 17 components with zero keyboard handlers, 8 components with zero role attributes |

### Cross-Audit Insights

- **Info panel is a cross-cutting concern:** CSS audit flags `.info-panel` scatter across 13 files (HIGH); Svelte audit flags InfoPanel with 0 `role=` attributes (HIGH). W21.2 added the role attribute. CSS consolidation deferred to W23.
- **Keyboard navigation is systemic:** 17 of 26 components have zero keyboard handlers. The 5 W21.2 hardened components improved this, but FocusCard and InfoPanel still have gaps.
- **Test consolidation is incremental:** 10/12 state-class tests consolidated in W21.1. The 2 remaining (navigation, journey) have unique patterns. Canonical regression tests are mergeable but not urgent.

---

## 4. Selected W22 Work Items

### Item 1: CSS demo_ui.css Orphan Cleanup — LOW, S effort

**Source:** CSS audit Smell 5
**Why:** Safe first step. 7 orphan selectors (~48 lines) with zero references in `src/` or `js/`. Proves the audit's orphan analysis is correct. No source code changes — CSS deletion only.

**Scope:** `css/demo_ui.css` (1 file)
**Acceptance criteria:**

- Delete orphan selectors: `.demo-end-toast`, `.demo-overlay-tag`, `.demo-tag-dot`, `.demo-toast-dismiss`, `.demo-toast-glyph`, `.demo-toast-text`, `.glass-panel`
- Keep `.demo-starter-chip` (1 reference) and `.view-toggle` (verify if `controls.css` already owns it)
- `grep -r "demo-end-toast\|demo-overlay-tag\|demo-tag-dot\|demo-toast-dismiss\|demo-toast-glyph\|demo-toast-text\|glass-panel" src/ js/` returns 0 matches
- `npm run test:microdemo` passes
- File shrinks from ~136 lines to ~30-40 lines

### Item 2: CSS .is-active Shared Modifier Documentation — LOW, S effort

**Source:** CSS audit Smell 3
**Why:** 1,584 occurrences of `.is-active` across 15 files with no clear ownership. Documentation-only change prevents future cascade confusion. Zero risk.

**Scope:** `docs/semantic-demo-css-ownership-map.md` (1 file)
**Acceptance criteria:**

- Add "Shared Modifiers" section to the ownership map
- Document `.is-active` with per-element ownership (e.g., `.search-results.is-active` → `search.css`, `.info-panel.is-active` → `layout_base.css`)
- Markdown lint passes

### Item 3: Navigation + Journey Test Migration — MEDIUM, M effort

**Source:** Test consolidation audit Opportunity 1 (remaining 2 files)
**Why:** 10/12 state-class tests consolidated in W21.1. The 2 remaining (navigation, journey) have unique T4-template patterns (`window` globals fallback chain, `delete` on mockState) but should still be migrated for consistency.

**Scope:** `tests/unit-active/navigation-state-class-migration.test.ts`, `tests/unit-active/journey-state-class-migration.test.ts` → new files
**Acceptance criteria:**

- Create `state-class-migration-navigation.test.ts` and `state-class-migration-journey.test.ts` (separate files, not merged with main scaffold — they have different mock patterns)
- Delete original 2 files
- `npx vitest run` passes with same assertion count
- No test coverage regression

### Item 4: FocusCard Keyboard Navigation + Loading State — MEDIUM, M effort

**Source:** Svelte audit §3 FocusCard
**Why:** W21.2 added basic a11y to FocusCard, but keyboard navigation is still absent (531 lines, zero `onkeydown` handlers). Keyboard-only users cannot navigate between card fields. Loading state also missing while `selectedRecord` resolves from 3-source fallback chain.

**Scope:** `src/components/FocusCard.svelte` (1 file)
**Acceptance criteria:**

- `role="article"` or `role="region"` with `aria-labelledby` on card wrapper
- Arrow key navigation between card sections (name → status → address → contact links)
- Loading skeleton while `selectedRecord` resolves
- `npm run check` passes with 0 new errors

### Item 5: Filters Keyboard Navigation — LOW, S effort

**Source:** Svelte audit §3 Filters
**Why:** Quick win. Filter chips have `aria-pressed` but no keyboard navigation. ~1 hour of work.

**Scope:** `src/components/Filters.svelte` (1 file)
**Acceptance criteria:**

- `role="group"` or `role="toolbar"` on filter container
- Arrow key navigation between filter chips within each group
- Live region announcing filter count changes
- `npm run check` passes with 0 new errors

### Item 6: SearchInput aria-expanded + searchbox Role — LOW, S effort

**Source:** Svelte audit §3 SearchInput
**Why:** Quick win. Input already has 11 `aria-*` attributes but missing `aria-expanded` to connect to results dropdown. ~1 hour.

**Scope:** `src/components/SearchInput.svelte` (1 file)
**Acceptance criteria:**

- `role="searchbox"` on input element
- `aria-autocomplete="list"` when results available
- `aria-expanded` toggles with results dropdown visibility
- `npm run check` passes with 0 new errors

### Items DEFERRED to W23+

| Item                                           | Source          | Why deferred                                                                                  |
| ---------------------------------------------- | --------------- | --------------------------------------------------------------------------------------------- |
| `.focus-stage` tail-authority rename (Smell 1) | CSS audit       | HIGH, L effort — rename + doc update + re-verify all contracts                                |
| `.info-panel` CSS consolidation (Smell 2)      | CSS audit       | HIGH, L effort — requires mobile browser proof at 390×844 for each moved rule across 13 files |
| Landscape breakpoint consolidation (Smell 4)   | CSS audit       | MEDIUM, M effort — requires live landscape proof at 8+ breakpoints across 6 files             |
| Component test foundation                      | Svelte audit §5 | HIGH, L effort — needs `@testing-library/svelte` setup decision, then writing 3-5 first tests |
| tests/unit/ legacy retirement (Opportunity 5)  | Test audit      | LOW, L effort — 10 files to port or retire + delete vitest.legacy.config.js                   |

---

## 5. Sequencing

### Wave 22.1 — CSS Quick Wins (parallel, LOW risk)

| Item                                   | Why first                         | Risk                               |
| -------------------------------------- | --------------------------------- | ---------------------------------- |
| **Item 1: demo_ui.css orphan cleanup** | Safe, fast, proves audit analysis | LOW — CSS deletion, no source refs |
| **Item 2: .is-active doc update**      | Docs-only, zero risk              | NONE — documentation only          |

Both operate on separate files. Dispatch in parallel.

### Wave 22.2 — Test Migration (after 22.1)

| Item                                            | Why here                                  | Risk                                          |
| ----------------------------------------------- | ----------------------------------------- | --------------------------------------------- |
| **Item 3: navigation + journey test migration** | Depends on clean test baseline from W21.1 | MEDIUM — test file changes, verify pass count |

Dispatch after 22.1 commits land. Sequential (both files may interact with test infrastructure).

### Wave 22.3 — Svelte Hardening (parallel, after 22.2)

| Item                                     | Why here                               | Risk                         |
| ---------------------------------------- | -------------------------------------- | ---------------------------- |
| **Item 4: FocusCard keyboard + loading** | Highest a11y gap remaining after W21.2 | MEDIUM — source modification |
| **Item 5: Filters keyboard nav**         | Quick win, independent of FocusCard    | LOW — source modification    |
| **Item 6: SearchInput aria-expanded**    | Quick win, independent of others       | LOW — source modification    |

Items 5 and 6 are independent of each other and of Item 4. Dispatch all three in parallel. All modify different `.svelte` files — no race condition.

---

## 6. Decomposition Strategy

### Parallel dispatches

- **22.1:** 2 workers (CSS deletion + docs update) — separate files, no collision
- **22.3:** 3 workers (FocusCard + Filters + SearchInput) — separate `.svelte` files, no collision

### Sequential dispatches

- **22.2:** 1 worker (navigation + journey test migration) — sequential after 22.1 to avoid confusion with test baseline

### Total dispatches: 6 workers across 3 sub-waves

| Wave | Workers | Model     | Files touched                                                                                                           |
| ---- | ------- | --------- | ----------------------------------------------------------------------------------------------------------------------- |
| 22.1 | 2       | mimo-v2.5 | `css/demo_ui.css`, `docs/semantic-demo-css-ownership-map.md`                                                            |
| 22.2 | 1       | mimo-v2.5 | `tests/unit-active/navigation-state-class-migration.test.ts`, `tests/unit-active/journey-state-class-migration.test.ts` |
| 22.3 | 3       | mimo-v2.5 | `src/components/FocusCard.svelte`, `src/components/Filters.svelte`, `src/components/SearchInput.svelte`                 |

---

## 7. Model Choice

**Default:** `opencode-go/mimo-v2.5` (paid, $0.001-0.003/worker)

- W20 success rate: 5/5 (100%)
- W21 parallel session used it successfully for a11y hardening
- Always available, no rate limits, fast execution

**Avoid:** `kilo/openrouter/owl-alpha` (free, rate-limited mid-task in W20), `openrouter/owl-alpha` (free, 0/4 in W20)

**Lesson reinforced:** Free models cost more in wasted dispatch cycles than they save. The $0.001/worker cost of mimo-2.5 is trivial.

---

## 8. Verification Gates

### For CSS items (22.1)

| Check                    | Command                                                       | Expected                |
| ------------------------ | ------------------------------------------------------------- | ----------------------- |
| Orphan selectors deleted | `grep -r "demo-end-toast\|demo-overlay-tag\|..." src/ js/`    | 0 matches               |
| Demo still works         | `npm run test:microdemo`                                      | Passes                  |
| File shrunk              | `wc -l css/demo_ui.css`                                       | ~30-40 lines (from 136) |
| Docs updated             | `grep -c "is-active" docs/semantic-demo-css-ownership-map.md` | >0 (new section exists) |
| Commit purity            | `git show --stat HEAD`                                        | Only expected files     |

### For test item (22.2)

| Check             | Command                                                         | Expected                          |
| ----------------- | --------------------------------------------------------------- | --------------------------------- |
| Tests pass        | `npx vitest run tests/unit-active/*state-class-migration*`      | Same assertion count as originals |
| Originals deleted | `ls tests/unit-active/navigation-state-class-migration.test.ts` | File not found                    |
| No regression     | `npm run test:unit`                                             | 0 new failures                    |
| Commit purity     | `git show --stat HEAD`                                          | Only test files                   |

### For Svelte items (22.3)

| Check                 | Command                            | Expected                                     |
| --------------------- | ---------------------------------- | -------------------------------------------- |
| svelte-check          | `npm run check`                    | 0 new errors                                 |
| A11y attributes       | Browser DevTools                   | `role=`, `aria-*` visible on target elements |
| Keyboard nav          | Manual test: Tab/Arrow keys        | Focus moves between interactive elements     |
| Loading state         | Manual test: clear + click node    | Skeleton appears before data resolves        |
| Commit purity         | `git show --stat HEAD`             | Only the target `.svelte` file               |
| Parallel session safe | `git status --short` before commit | No unexpected M-flagged files                |

---

## 9. Friction Patterns to Avoid

| Pattern                             | W20/W21 lesson                      | W22 mitigation                                       |
| ----------------------------------- | ----------------------------------- | ---------------------------------------------------- |
| `pi_background_jobs action: "poll"` | Invalid action, workers burn 5+ min | Hard rule in every subagent prompt                   |
| Trusting docs as code truth         | Cross-import map wrong 3 times      | Verify orphans with `rg` before CSS deletion         |
| Free model rate limits              | owl-alpha 1/6 success rate          | Use mimo-2.5 for all W22 dispatches                  |
| Subagent commit mismatch            | Owl-5 committed 1 file, claimed 5   | Workers run `git show --stat HEAD` and confirm       |
| Same-file race conditions           | Multiple workers = conflicts        | Each worker owns separate files                      |
| Large file writes                   | Mimo-2.5 stuck on 2000+ line files  | Max 800 lines per write; use Edit for larger changes |
| Deep-relative import stragglers     | 2 rounds needed in W20              | Run completeness check after each round              |

---

## 10. Parallel Session Coordination

**Current state:** HEAD is `6f05d03`. The parallel session completed W21.2 hardening (commit `14a9852`) and W21.3 cleanup (`7a89261`). No active parallel session work visible in the last 30 commits.

**Rules:**

1. Before any `git commit`, run `git status --short`
2. If files you didn't touch show `M` flags → the parallel session is active
3. Use `git commit --only <pathspec>` to avoid pulling in their WIP
4. Do not touch files the parallel session may be modifying
5. W22 items operate on different files than any known parallel session work — no collision expected
6. If the parallel session commits during W22 execution, rebase W22 work on the new `origin/master` before pushing

---

## 11. Open Questions

1. **Component test framework decision:** The Svelte audit recommends component unit tests (HIGH, L effort) but this blocks on a framework choice: `@testing-library/svelte` + Vitest vs Playwright component testing. Defer to W23 until decision is made. Main lane recommendation: `@testing-library/svelte` + Vitest for unit-level, Playwright for visual/interaction contracts.

2. **Should navigation + journey tests be separate files or merged into the main scaffold?** The main scaffold (`state-class-migration.test.ts`) already has 10 stores parameterized. Navigation and journey use different mock patterns (`window` globals fallback chain, `delete` on mockState). Recommendation: separate files to keep the mock patterns clean.

3. **AGENTS.md component count drift:** 26 components exist, AGENTS.md lists 21. Five added since last sync: DevGui, FocusPocketA11y, MapView, SpectorInspector, Toast. Should this be updated in W22 or deferred?

4. **W21 retrospective:** Not yet written. Should be created as part of W22 docs or as a standalone task? The W20 retrospective is a good template.

5. **FocusCard loading state source:** The component resolves `selectedRecord` from 3 sources (Svelte store, body attrs, legacy `__APP_STATE__`). Should the loading state gate on all 3 being evaluated, or show content as soon as any source resolves? Recommend: gate on store resolution (fastest path), degrade to body attrs only if store is empty.

---

## 12. References

| Document                    | Path                                                  | Relevance                                                     |
| --------------------------- | ----------------------------------------------------- | ------------------------------------------------------------- |
| W20 retrospective           | `notes/w20-retrospective-2026-06-17.md`               | Lessons learned, model choice, subagent patterns              |
| CSS ownership investigation | `notes/w21-css-ownership-investigation-2026-06-17.md` | 5 smells, orphan analysis, cascade audit                      |
| Test consolidation audit    | `notes/w21-test-consolidation-audit-2026-06-17.md`    | 5 opportunities, test inventory, organization recommendations |
| Svelte component audit      | `notes/w21-svelte-component-audit-2026-06-17.md`      | 26 components, quality matrix, hardening priorities           |
| W21 charter                 | `docs/w21-charter-2026-06-17.md`                      | W21 plan template, verification gates pattern                 |
| CSS ownership map           | `docs/semantic-demo-css-ownership-map.md`             | To update with .is-active shared modifier section             |
| Mobile state ownership      | `docs/semantic-demo-mobile-state-ownership.md`        | Reference for CSS cascade decisions                           |

---

## Appendix: File Inventory at Charter Time

| Metric                      | Count     | Notes                                                                             |
| --------------------------- | --------- | --------------------------------------------------------------------------------- |
| HEAD commit                 | `6f05d03` | `chore(focus-pocket): simplify to 3D-only DOM anchor`                             |
| js/modules/ `.ts` files     | 59        | Engine kernel (active runtime)                                                    |
| src/components/             | 26        | 21 documented + 5 new (DevGui, FocusPocketA11y, MapView, SpectorInspector, Toast) |
| CSS files                   | 25        | 18,747 total lines                                                                |
| Test files                  | 302       | 217 root + 67 unit-active + 10 unit-legacy + 8 other                              |
| vitest pass/fail            | 711/3+    | 3+ failures (down from 9 after W21.1 readiness gate restructure)                  |
| W21 audit docs              | 3         | 904 total lines of source material                                                |
| State-class-migration tests | 12        | 10 consolidated, 2 standalone (navigation, journey)                               |

---

_Charter written 2026-06-17 by main lane. 6 work items across 3 sub-waves, mimo-2.5 paid dispatches._
