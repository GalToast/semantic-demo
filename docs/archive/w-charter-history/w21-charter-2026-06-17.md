# Wave 21 Charter — 2026-06-17

**Author:** main lane + mimo-2.5 subagent dispatches
**Status:** charter synthesis complete, awaiting execution
**Scope:** CSS ownership cleanup, test suite hardening, Svelte component a11y + accessibility
**Estimated effort:** 3-5 subagent dispatches over 2-3 days
**Parallel session note:** 13 files M-flagged in working tree — use `git commit --only` for all commits

---

## 1. Wave 21 Goals

Wave 21 picks up after the W20 cleanup arc (24 commits, 7 file deletions, 5 regression tests). Three READMOSTLY audits landed today identifying fresh seams: CSS ownership smells, test consolidation opportunities, and Svelte component hardening needs. W21 should: (1) fix the misleading test failures that poison `npm run test:unit` output, (2) eliminate dead CSS selectors and clarify the cascade authority contract, (3) close the most critical accessibility gaps in the user-facing Svelte components, and (4) consolidate duplicated test boilerplate to reduce maintenance surface. The goal is a quieter test suite, a cleaner CSS ownership map, and a more accessible DOM UI — not a comprehensive rewrite of any layer.

---

## 2. Source Audits

| #   | Audit                                                 | Lines | Key Findings                                                                                                                                                                                        |
| --- | ----------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `notes/w21-css-ownership-investigation-2026-06-17.md` | 301   | 5 smells: `.focus-stage` tail-authority inversion (HIGH), `.info-panel` 13-file scatter (HIGH), `.is-active` proliferation (MEDIUM), landscape breakpoints (MEDIUM), `demo_ui.css` orphans (LOW)    |
| 2   | `notes/w21-test-consolidation-audit-2026-06-17.md`    | 257   | 5 opportunities: permanently-failing readiness gate (HIGH), 12 duplicate state-class tests (MEDIUM), parity test overlap (MEDIUM), canonical regression overlap (MEDIUM), stale legacy config (LOW) |
| 3   | `notes/w21-svelte-component-audit-2026-06-17.md`      | 346   | 26 components, 0 unit tests, FocusCard keyboard gap (HIGH), InfoPanel role gap (HIGH), component test foundation (HIGH), Filters keyboard nav (MEDIUM), SearchInput aria-expanded (MEDIUM)          |

**Cross-audits:** The CSS and Svelte audits both flag the info panel as a high-impact surface (13-file CSS scatter + zero `role=` attributes). The test and CSS audits both identify dead/orphan artifacts to clean up. These cross-cuts inform the sequencing below.

---

## 3. Selected Work Items (5 highest-impact)

### Item 1: ~~Fix or Skip w20-wave4-readiness-regression~~ — RETIRED 2026-06-17

**Status:** **Retired.** W31 P1-A confirmed in `tmp/w31-p1a-readiness-fix/` that this test passes 14/14 as a regression gate — the W24–W29 cleanup arc organically completed the work this item was tracking. No edit produced. The 9-failure baseline cited below has shifted: vitest now reports 88 suites passed + 1 unrelated failure (focus-port-parity.test.ts broken-import, tracked as W31 P2-C).

**Source:** `notes/w21-test-consolidation-audit-2026-06-17.md` §2 Opportunity 2 (was "permanently-failing readiness gate - HIGH")
**Why now:** ~~This test contributes 5+ of the 9 total vitest failures.~~ Retired: the parallel session's W29 cleanup landed before dispatch; the test is now green.
**Scope:** `tests/unit-active/w20-wave4-readiness-regression.test.ts` (1 file)
**Effort:** S
**Subagent dispatch:** Investigate which of the 10 expected-deleted files still exist in `js/modules/`. If the file exists, the assertion is wrong (parallel session restored it). If the file is gone, the test should pass. Either fix the assertions to match current reality, or `describe.skip()` the failing sections with a tracking comment.
**Acceptance:** `npx vitest run tests/unit-active/w20-wave4-readiness-regression.test.ts` exits 0 failures. `npm run test:unit` shows 0 or fewer failures than before.

---

### Item 2: FocusCard Keyboard Navigation + Loading State — HIGH

**Source:** `notes/w21-svelte-component-audit-2026-06-17.md` §3 FocusCard
**Why now:** FocusCard is the primary user-facing business detail card (531 lines). It has zero keyboard handlers — keyboard-only users cannot interact with business details at all. This is the single highest-impact a11y gap in the component set. It also lacks a loading state while the multi-source `selectedRecord` fallback chain resolves.
**Scope:** `src/components/FocusCard.svelte` (1 file)
**Effort:** M
**Subagent dispatch:** Add `role="article"` or `role="region"` to the card wrapper with `aria-labelledby` pointing to the business name heading. Add keyboard navigation between card sections (name → status → address → contact links) using arrow keys. Add a loading/skeleton state while `selectedRecord` resolves from the 3-source fallback chain (lines 157-176). Add `aria-describedby` linking status badges to their visual indicators. Ensure contact links have proper `aria-label` and open in new tab with `rel="noopener"`.
**Acceptance:** FocusCard renders with `role="article"` and `aria-labelledby`. Arrow keys navigate between card sections. A loading skeleton appears before data resolves. `npm run check` passes with 0 new errors.

---

### Item 3: Delete demo_ui.css Orphan Selectors — LOW (quick win, high signal)

**Source:** `notes/w21-css-ownership-investigation-2026-06-17.md` §2 Smell 5 + §4
**Why now:** 6 of 9 selectors in `demo_ui.css` have zero references in `src/` or `js/`. Dead CSS is noise. This is a safe, fast deletion that reduces the CSS surface and proves the ownership audit's orphan analysis is trustworthy. Also clarify the misleading `strands.css:72` comment while touching CSS.
**Scope:** `css/demo_ui.css` (1 file), `css/strands.css` (1 comment fix)
**Effort:** S
**Subagent dispatch:** Delete the 6 orphan selectors from `demo_ui.css` (`.demo-end-toast`, `.demo-overlay-tag`, `.demo-tag-dot`, `.demo-toast-dismiss`, `.demo-toast-glyph`, `.demo-toast-text`, `.glass-panel`). Keep `.demo-starter-chip` if it has references. Verify `.view-toggle` ownership is in `controls.css` and remove the duplicate from `demo_ui.css` if confirmed dead. Fix the misleading comment at `strands.css:72` — reword from "Using `!important` is appropriate here" to "Selector order, not `!important`: reduced-motion takes precedence via cascade order." Run `npm run test:microdemo` to verify demo still works.
**Acceptance:** `demo_ui.css` shrinks from ~136 lines to ~30 lines (only `.demo-starter-chip`). `grep -r "demo-end-toast\|demo-overlay-tag\|demo-tag-dot\|demo-toast-dismiss\|demo-toast-glyph\|demo-toast-text\|glass-panel" src/ js/` returns 0 matches. `npm run test:microdemo` passes. `strands.css:72` comment no longer mentions `!important`.

---

### Item 4: InfoPanel Role Attribute + Loading Skeleton — HIGH

**Source:** `notes/w21-svelte-component-audit-2026-06-17.md` §3 InfoPanel + `notes/w21-css-ownership-investigation-2026-06-17.md` §2 Smell 2
**Why now:** InfoPanel is 828 lines — the largest Svelte component. It has zero `role=` attributes despite being the primary information surface. The CSS audit also flags `.info-panel` as scattered across 13 files. Adding the landmark role is the highest-leverage a11y fix for this component, and the loading skeleton addresses the "no loading UX" gap (only 3 of 26 components have loading indicators).
**Scope:** `src/components/InfoPanel.svelte` (1 file)
**Effort:** M
**Subagent dispatch:** Add `role="complementary"` to the panel wrapper (it already has `aria-label` but no role). Add a loading skeleton while `effectiveSurface` and `selectedRecord` resolve. Add `aria-current` or equivalent to indicate which surface section is active. Do NOT attempt to consolidate the CSS scatter (Smell 2) — that's a separate high-risk wave requiring mobile browser proof. The CSS consolidation is deferred.
**Acceptance:** InfoPanel renders with `role="complementary"`. A loading skeleton appears before surface data resolves. `npm run check` passes with 0 new errors.

---

### Item 5: Consolidate 12 State-Class-Migration Tests — MEDIUM

**Source:** `notes/w21-test-consolidation-audit-2026-06-17.md` §2 Opportunity 1
**Why now:** 12 test files follow an identical template: mock `@lib/state/app.svelte.ts`, import a store, verify getter read paths. The boilerplate is ~60-80 lines per file. Consolidating to 1 parameterized file eliminates ~300 lines of duplicated setup and makes pattern updates one-file. This reduces the test maintenance surface and makes the state-class migration pattern easier to understand.
**Scope:** `tests/unit-active/*-state-class-migration.test.ts` (12 files → 1 file)
**Effort:** M
**Subagent dispatch:** Create `tests/unit-active/state-class-migration.test.ts` with an array of `{ storeName, importFn, fields, mockShape }` entries. Each entry becomes a `describe()` block. The shared mock factory, setup, and teardown live once. Delete the 12 original files after verifying the consolidated test passes all assertions. Run `npx vitest run tests/unit-active/state-class-migration.test.ts` to verify.
**Acceptance:** 12 files replaced by 1 file. `npx vitest run tests/unit-active/state-class-migration.test.ts` passes with the same assertion count as the sum of the 12 originals (~150 assertions). No test coverage regression.

---

## 4. Sequencing

### Wave 21.1 — Quick Wins + Test Suite Green (parallel dispatches)

| Item                                   | Why first                                                                                               |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| **Item 1: Fix readiness regression**   | Unblocks test suite trust. 5+ failures → 0. Must land before any test consolidation to avoid confusion. |
| **Item 3: Delete demo_ui.css orphans** | Safe, fast, high-signal. Proves audit analysis is correct. No dependencies.                             |

These two are fully independent and can run in parallel. Both are S-effort.

### Wave 21.2 — Accessibility Fixes (parallel dispatches)

| Item                                     | Why here                                                                         |
| ---------------------------------------- | -------------------------------------------------------------------------------- |
| **Item 2: FocusCard keyboard + loading** | Highest a11y gap. 531-line card, zero keyboard handlers.                         |
| **Item 4: InfoPanel role + loading**     | Largest component, zero `role=` attributes. Both are Svelte component hardening. |

These two are independent (different files) and can run in parallel. Both are M-effort.

### Wave 21.3 — Test Consolidation (sequential)

| Item                                      | Why last                                                                                                                 |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| **Item 5: Consolidate state-class tests** | Depends on test suite being green (Item 1). Large file deletion (12 → 1) needs clean baseline. Run after Items 1-4 land. |

This is M-effort and should run after 21.1 and 21.2 to avoid merge conflicts with test changes.

---

## 5. Decomposition Strategy

### Parallel dispatches (21.1 and 21.2)

Items 1+3 are independent — dispatch both to mimo-2.5 workers simultaneously. Items 2+4 are independent — dispatch both after 21.1 lands.

### Sequential dispatches (21.3)

Item 5 depends on Item 1 being landed (test suite must be green before consolidating). Dispatch after 21.1 commit is verified.

### READMOSTLY work is safe to parallelize

Items 1, 3, and 5 are read-heavy (investigate, verify, consolidate). Item 1 reads test files and fixes assertions. Item 3 reads CSS, deletes dead selectors, verifies demo. Item 5 reads 12 test files, creates 1 consolidated file.

### Source-modifying work needs pre-flight + commit

Items 2 and 4 modify Svelte components. Workers must:

1. Report exposed tools before starting
2. Run `git status --short` before committing
3. Use `git commit --only <pathspec>`
4. Run `npm run check` to verify svelte-check passes
5. Run `git show --stat HEAD` after committing

### What NOT to do in W21

- **Do NOT consolidate `.info-panel` CSS** (Smell 2 from CSS audit) — requires mobile browser proof at 390×844 for each moved rule. Defer to a dedicated CSS wave.
- **Do NOT consolidate landscape breakpoints** (Smell 4 from CSS audit) — requires live landscape proof at each breakpoint. Defer.
- **Do NOT add component unit tests** (Svelte audit §5 Item 5) — highest-leverage investment but L-effort, needs `@testing-library/svelte` setup decision. Defer to W22.
- **Do NOT attempt to rename `focus_stage.css`** (Smell 1 from CSS audit) — L-effort, requires updating all docs and contract tests. Defer.
- **Do NOT reorganize root-level `.mjs` contract tests** (Test audit §5) — 155 files, all npm scripts. Separate wave.

---

## 6. Model Choice

**Default:** `opencode-go/mimo-v2.5` (paid, $0.001-0.003/worker)

- W20 success rate: 5/5 (100%)
- Always available, no rate limits
- Fast execution on implementation tasks

**Avoid:** `kilo/openrouter/owl-alpha` (free, 1/2 success rate in W20 — rate limited mid-task)
**Avoid:** `openrouter/owl-alpha` (free, 0/4 success rate in W20 — 429 on every attempt)

**Lesson from W20:** Free models cost more in wasted dispatch cycles than they save in dollars. The $0.001/worker cost of mimo-2.5 is trivial. Default to paid for any task that matters.

---

## 7. Verification Gates

### For READMOSTLY items (1, 3, 5)

| Check                   | Command                                                          | Expected                                            |
| ----------------------- | ---------------------------------------------------------------- | --------------------------------------------------- |
| Test suite green        | `npx vitest run`                                                 | 0 new failures (Item 1: fewer failures than before) |
| Dead CSS confirmed dead | `grep -r "<selector>" src/ js/`                                  | 0 matches for each deleted selector                 |
| Demo still works        | `npm run test:microdemo`                                         | Passes                                              |
| Consolidated tests pass | `npx vitest run tests/unit-active/state-class-migration.test.ts` | Same assertion count as 12 originals                |
| Commit purity           | `git show --stat HEAD`                                           | Only expected files in the commit                   |

### For SOURCE-MOD items (2, 4)

| Check                   | Command                             | Expected                                          |
| ----------------------- | ----------------------------------- | ------------------------------------------------- |
| svelte-check            | `npm run check`                     | 0 new errors                                      |
| Component renders       | Load app, navigate to focus view    | FocusCard/InfoPanel visible with new attributes   |
| A11y attributes present | Browser DevTools inspection         | `role="article"` / `role="complementary"` visible |
| Keyboard navigation     | Tab/arrow keys in FocusCard         | Focus moves between card sections                 |
| Loading state           | Clear selected node, click new node | Skeleton appears before data resolves             |
| Commit purity           | `git show --stat HEAD`              | Only the target `.svelte` file                    |
| Parallel session safe   | `git status --short` before commit  | No unexpected M-flagged files in commit           |

---

## 8. Friction Patterns to Avoid (from W20 Retrospective)

| Pattern                                 | W20 lesson                                   | W21 mitigation                                                      |
| --------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------- |
| `pi_background_jobs action: "poll"`     | Invalid action, workers burn 5+ min retrying | Hard rule in every subagent prompt: "DO NOT use pi_background_jobs" |
| Trusting docs as code truth             | Cross-import map was wrong 3 times           | Verify canonicals with `rg "^export"` before acting on audit claims |
| Owl-alpha rate limits                   | 0/4 success on implementation tasks          | Use mimo-v2.5 for all W21 dispatches                                |
| Subagent commit message vs actual files | Owl-5 committed 1 file, claimed 5            | Workers must run `git show --stat HEAD` and confirm                 |
| Deep-relative import stragglers         | 2 rounds needed for cleanup                  | Run completeness check after each round                             |
| Parallel session WIP in commits         | 13 M-flagged files in working tree           | `git commit --only <pathspec>` for every commit                     |

### Subagent prompt template (proven in W20, reuse for W21)

```
<TASK>Specific scoped task with file ownership boundaries</TASK>
<RULES>
1. Report exposed tools before starting work
2. Use git commit --only <pathspec> for every commit
3. Run git status --short before committing
4. Run git show --stat HEAD after committing
5. DO NOT use pi_background_jobs — use git status instead
6. If you find a bug outside your scope, STOP and report it
</RULES>
<VERIFICATION>Exact commands to run before marking done</VERIFICATION>
```

---

## 9. Parallel Session Coordination

**Current state:** 13 files M-flagged in working tree (lifecycle.ts, journey-compass-controller.ts, journey-semantic-overlay.ts, lifecycle-modes.ts, url-state.ts, 6 test files, 2 src/ files). The parallel session is running lifecycle cleanup work.

**Rules:**

1. Before any `git commit`, run `git status --short`
2. If files you didn't touch show `M` flags → the parallel session is active
3. Use `git commit --only <pathspec>` to avoid pulling in their WIP
4. Do not touch `js/modules/lifecycle.ts`, `js/modules/journey-compass-controller.ts`, or `js/modules/journey-semantic-overlay.ts` — those are parallel session WIP
5. Items 1, 2, 3, 4, 5 all operate on different files than the parallel session — no collision expected
6. If the parallel session commits during W21 execution, re-run `git status --short` before your next commit

---

## 10. Open Questions

1. **Is `tests/unit/search-tokenizer-parity.test.js` safe to delete?** The audit says it's superseded by the `.ts` port in `tests/unit-active/`, but we should verify the `.ts` version covers all the same test cases before deleting. Defer to W22 (part of the larger `tests/unit/` retirement).

2. **Should `demo_ui.css` be deleted entirely after orphan removal?** If only `.demo-starter-chip` remains, it might be simpler to move that one selector to another file. The audit suggests this but doesn't commit to it. Main lane decision: keep the file with 1 selector for now — moving it is low priority.

3. **Component test framework:** The Svelte audit asks whether to use `@testing-library/svelte` + Vitest or Playwright component testing. This decision blocks W22's component test foundation work. Main lane recommendation: `@testing-library/svelte` + Vitest for unit-level component tests, Playwright for visual/interaction contract tests. Confirm with user before W22.

4. **Should the 5 new components (DevGui, FocusPocketA11y, MapView, SpectorInspector, Toast) be added to AGENTS.md?** The scaffold status table is stale at 21 components. These 5 were added since the last sync. Defer to W22 doc cleanup.

5. **InfoPanel CSS consolidation timing:** The CSS audit flags `.info-panel` scatter across 13 files as HIGH severity, but fixing it requires mobile browser proof. Should this be a dedicated CSS wave (W23) or part of a broader "info panel ownership" initiative? Recommend dedicated CSS wave — the a11y fix (Item 4) is separate from the CSS consolidation.

---

## Appendix: File Inventory at Charter Time

| Metric                     | Count     | Notes                                                |
| -------------------------- | --------- | ---------------------------------------------------- |
| HEAD commit                | `a9d0ea6` | `docs(notes): W21 test consolidation audit`          |
| js/modules/ files          | 59        | `.ts` engine kernel files                            |
| src/components/            | 26        | 21 documented in AGENTS.md + 5 new                   |
| CSS files                  | 25        | 18,747 total lines                                   |
| Test files                 | 302       | 217 root + 67 unit-active + 10 unit-legacy + 8 other |
| vitest pass/fail           | 711/9     | 9 failures (5+ from readiness gate)                  |
| Parallel session M-flagged | 13        | lifecycle, journey-compass, tests                    |
| W21 audit docs             | 3         | 904 total lines of source material                   |

---

_Charter written 2026-06-17 by main lane. 5 work items, 3 sub-waves, mimo-2.5 paid dispatches._
