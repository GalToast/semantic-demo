# W17 Charter — Kernel Bridge Wave + Legacy Cleanup

**Date:** 2026-06-17
**Duration target:** 1 session (~1 hour core + subagent churn)
**Goal:** Close out Phase II kernel ports — finish the remaining `js/modules/` files that src/ still imports.

---

## Starting from (today's landing state)

| Metric                            | Status                                       |
| --------------------------------- | -------------------------------------------- |
| `js/modules/bindings/`            | ✅ **DELETED**                               |
| `js/modules/` remaining files     | **~30**                                      |
| `src/` imports from `js/modules/` | **~20** (Bridge re-exports + direct imports) |
| `svelte-check`                    | ✅ 0 errors, 0 warnings                      |

---

## Wave 1: Recon remaining js/modules kernel (subagent)

**Dispatch target:** `js/modules/{map-state,view-controller,composition-state,journey,lifecycle,loading-ui,scene-reveal}.ts`

Like the event-bindings recon before it, this subagent should:

1. List ALL exports for each file
2. Find ALL consumers (in `js/modules/`, `src/`, `tests/`)
3. Categorize: **A) Port** (has src/ consumers), **B) Inline** (only used internally), **C) Delete** (dead code)
4. Report: `tmp/w17-kernel-recon/REPORT.md`

Priority files (ordered by consumer count in src/):

1. `js/modules/map-state.ts` — `cursor.ts` imports it, 10+ exports
2. `js/modules/journey.ts` — `cursor.ts` imports it, massive surface area
3. `js/modules/view-controller.ts` — imported by lifecycle + bridge
4. `js/modules/composition-state.ts` — imported by lifecycle + bridge
5. `js/modules/scene-reveal.ts` — imported by engine
6. `js/modules/lifecycle.ts` — already pruned 37 dead exports, still has active ones
7. `js/modules/loading-ui.ts` — might already be canonical

---

## Wave 2: Port the top 3 kernel files

Based on recon, dispatch 1-2 port subagents for:

- **map-state** → `src/lib/orchestration/map-state.ts` (already exists? verify)
- **view-controller** → `src/lib/orchestration/view-controller.ts` (already exists? verify)
- **composition-state** → `src/lib/orchestration/composition-state.ts`
- **scene-reveal** → `src/lib/engine/scene-reveal.ts` (already exists? verify)

For each:

- Copy to canonical path (or update if already exists)
- Update all consumers in `src/` to point to canonical
- Verify `grep "from.*js/modules/<name>" src/ tests/` → 0 results
- Delete legacy file

---

## Wave 3: Thin the bridges

After porting, many `*-bridge.ts` files in `src/lib/engine/` will re-export from canonical only. For any bridge with zero active downstream consumers at `src/`, **delete it**.

## Wave 4: Fix remaining direct imports

After the above, run:

```bash
grep -r "from.*js/modules" src/ tests/ --include="*.ts" --include="*.svelte"
```

Anything remaining should be:

- Fixed with canonical import
- Or documented as blocked (and why)

---

## Verification at end of session

- [ ] `svelte-check` still: 0 errors, 0 warnings
- [ ] `npm run build:svelte` passes
- [ ] `grep -r "from.*js/modules" src/ | grep -v "engine/adapters-bridge" | wc -l` → target: < 10
- [ ] All new canonical files pass `svelte-check`

---

## Exit criteria (what "done" looks like)

- `js/modules/` file count reduced to < 20 (from ~30)
- `src/` imports from `js/modules/` reduced to < 10 (from ~20)
- Zero new svelte-check errors introduced
- All changes committed with descriptive messages

---

## What W18 looks like (preview)

After W17 closes Phase II, W18 tackles:

1. **The final 10 js/modules files** — likely dead code + thin bridges
2. **Test modernization** — update tests/unit-active/ to use canonical paths
3. **Final purge** — delete `js/modules/` directory when empty
4. **Documentation sweep** — update README, architecture docs

---

_Charter written: 2026-06-17 00:40 UTC_
_Next session should start with this charter in hand_
