# W38 Charter — Post-Migration Polish: README + Docs Archive + CI Polish + Performance Budget

**Date:** 2026-06-17
**Status:** Migration structurally complete. Build passes, svelte-check 0/0, deploy scripts verified, CI verified, tests clean.
**Rule:** Do NOT touch parallel-session modified files (AGENTS.md, docs/semantic-demo-design-tokens.md, tests/ts-js-drift-contract.mjs).

---

## 🎯 W38 Goals

### Goal 1: README Refresh

Update README.md to reflect the post-migration state. Remove the 1 stale `js/modules/` reference, refresh architecture description, and make it accurate for new contributors.

### Goal 2: Docs Archive

Move 20+ migration history docs from `docs/` root to `docs/archive/` to declutter the main docs directory. Keep them accessible but not in the primary workspace.

### Goal 3: Visual Regression CI Polish

Finish and verify the `tests/visual-regression.test.ts` script from W36. Ensure it runs cleanly, produces meaningful output, and can be used for future regression detection.

### Goal 4: Performance Budget Document

Write `docs/performance-budget-2026-06-17.md` with formal targets based on measured data from W35 deploy verification.

---

## 🚦 Coordination

1. Safe zones: `README.md`, `docs/`, `tests/`, `notes/`
2. Do NOT touch: AGENTS.md, docs/semantic-demo-design-tokens.md, tests/ts-js-drift-contract.mjs

## Success Criteria

- [ ] README.md updated and accurate
- [ ] Migration docs archived to `docs/archive/`
- [ ] Visual regression test script runs and passes
- [ ] Performance budget written and committed
- [ ] Build passes, svelte-check 0/0
- [ ] All committed and pushed
