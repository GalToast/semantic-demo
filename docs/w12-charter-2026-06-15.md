# W12 Charter — Pre-emption Sweep Results

> **Generated:** 2026-06-15
> **Methodology:** 3 parallel subagent dispatches (orchestration, journey, fix/* branch comparison) under the Subagent Throughput Doctrine codified in AGENTS.md 2026-06-15
> **Inputs:** `tmp/w12-preempt-orchestration/INVENTORY.md` (270 lines), `tmp/w12-preempt-journey/INVENTORY.md` (470+ lines), `tmp/w12-preempt-branches/COMPARISON.md` (8 sections)
> **Doctrine validation:** 3 subagents, ~$0.02 total, ~7 min wall time, all in scope, real subsumption found

---

## TL;DR

The W11 engine-port arc (T1–T11) is **functionally complete** on master. Master is at `7abdeaa` (with `a571a0b` A2-6 and `ae50ea0` garbage cleanup merged). 645/645 unit tests pass, svelte-check 0/0, build 6s.

What remains for W12 is **cleanup, not new work**:

1. **HIGH (immediate):** Delete `parity-attrs.ts` (472 LOC superseded) + port `journey-webgl-bridge.ts` dependencies to `src/lib/journey/` (~310 LOC)
2. **MEDIUM (audit):** 14 no-op stubs in `lifecycle.ts`, `window-actions.ts` dual imports
3. **LOW (cosmetic):** Event-bus triple-chain, cross-layer imports, duplicate triggers import
4. **Branches:** All 4 fix/* branches are FULLY SUBSUMED. Safe to delete.

---

## W12 Ticket Breakdown

### W12-T1: Delete `parity-attrs.ts` (HIGH, ~30 min)

**File:** `src/lib/orchestration/parity-attrs.ts` (472 LOC, stale)
**Replacement:** `parity-attrs.svelte.ts` (490 LOC, Svelte 5 runes)
**Action:**
1. Update `src/App.svelte:46` import from `@lib/orchestration/parity-attrs` → `@lib/orchestration/parity-attrs.svelte`
2. Update all other consumers of `installParityAttributeSync` (search for `from '@lib/orchestration/parity-attrs'`)
3. Delete `src/lib/orchestration/parity-attrs.ts`
4. Run svelte-check + test:unit to confirm parity behavior preserved
**Lines removed:** 472
**Risk:** LOW — both files export the same `PARITY_ATTRIBUTES` manifest and `installParityAttributeSync` signature; the `.svelte.ts` uses `$effect.root()` instead of imperative `.subscribe()` (semantically equivalent)

### W12-T2: Port `journey-webgl-bridge.ts` dependencies (HIGH, ~3 hours)

**Current state:** 9 bridges in `src/lib/engine/`. 8 are 1-3 line re-exports. **`journey-webgl-bridge.ts` is the LAST bridge still importing from `js/modules/`** — it imports from 3 legacy files.

**Legacy files to port:**
- `js/modules/journey-webgl.ts` (41 LOC, barrel)
- `js/modules/thread-inspector-webgl.ts` (~290 LOC, WebGL line rendering for thread inspector)
- `js/modules/inspected-strand-overlay-adapter.ts` (14 LOC, bare adapter)
- `js/modules/route-arrival-overlay-adapter.ts` (29 LOC, side-effect wiring)

**Action:**
1. Port `js/modules/thread-inspector-webgl.ts` to `src/lib/journey/thread-inspector-webgl.ts` (~290 LOC ported)
2. Fold `inspected-strand-overlay-adapter.ts` (14 LOC) and `route-arrival-overlay-adapter.ts` (29 LOC) into `src/lib/journey/webgl.ts` barrel
3. Update `journey-webgl-bridge.ts` to re-export from `src/lib/journey/` (not `js/modules/`)
4. Verify `journey.ts` and `JourneyChrome.svelte` consumers still work
**Lines affected:** ~310 LOC ported, ~40 LOC bridge + adapters simplified
**Risk:** MEDIUM — thread-inspector-webgl has WebGL shader code; needs testing

### W12-T3: Audit 14 no-op stubs in `lifecycle.ts` (MEDIUM, ~1 hour)

**File:** `src/lib/orchestration/lifecycle.ts`
**Stubs to audit:**
- `setSemanticLaneUiState()`, `recordSemanticLaneSnapshot()`, `setSemanticLaneOpsMode()`, `refreshSemanticLaneOpsSummary()`
- `syncSearchStatusForFocus()`, `hideSummaryCard()`, `setSemanticGuideButtonState()`, `showSummaryCard()`, `requestSemanticGuide()`
- `syncSemanticDiveUi()` (intentional no-op — parity-attrs owns it)
- `showSemanticThreadsDetail()` (no-op — engine bridge handles)
- `getMobileSearchSheetDetail()` (returns "")
- `probeSemanticLane()` and related

**Action:** For each stub, grep for callers. If zero callers, delete the function. If callers exist, either wire to the real implementation or document the no-op intent.

### W12-T4: Consolidate `window-actions.ts` dual imports (MEDIUM, ~1 hour)

**File:** `src/lib/orchestration/window-actions.ts` (332 LOC)
**Issue:** Imports from `window-actions-bridge.ts` (which re-exports from `js/modules/`), but some of the same functions are also re-exported from `orchestration/lifecycle.ts`
**Action:** Audit whether `window-actions.ts` can import directly from orquestration re-exports instead of bridge re-exports. Consolidate to one source.

### W12-T5: Collapse event-bus triple-chain (LOW, ~30 min)

**Files:**
- `src/lib/orchestration/event-bus.ts` (195 LOC, canonical)
- `src/lib/event-bus.ts` (14 LOC, thin re-export shim)
- `src/lib/engine/event-bus-bridge.ts` (8 LOC, thin re-export shim)

**Action:**
1. Redirect 6 consumers of `@lib/engine/event-bus-bridge` to import directly from `@lib/orchestration/event-bus`
2. Delete `src/lib/event-bus.ts` and `src/lib/engine/event-bus-bridge.ts`
**Lines removed:** 22

### W12-T6: Fix `adapter-deps.ts:27` cross-layer import (LOW, ~15 min)

**File:** `src/lib/orchestration/adapter-deps.ts:27`
**Issue:** Direct `import { _getSelectedBusinessRoleLabel } from '../../../js/modules/role-label'` — bypasses bridge contract
**Action:** Route through a bridge or port `js/modules/role-label` to `src/lib/`

### W12-T7: Fix duplicate `@lib/orchestration/triggers` import in App.svelte (LOW, ~5 min)

**File:** `src/App.svelte`
**Issue:** Imports `@lib/orchestration/triggers` twice (lines 24 and 49)
**Action:** Verify intentional, remove duplicate if not

### W12-T8: Tighten `adapters.ts` types (LOW, ~1 hour)

**File:** `src/lib/orchestration/adapters.ts`
**Issue:** `// @ts-nocheck` directive with `[key: string]: unknown` escape hatches
**Action:** Remove `@ts-nocheck`, tighten the `AdapterDeps` interface, resolve type mismatches that stalled from W11-T7

### W12-T9: Delete 4 fully-subsumed fix/* branches (CLEANUP, ~15 min)

**Branches to delete (all 0-1 commits ahead of master, work already in master):**
- `fix/canvas-tdz-nemotron` (tip in master, 0 ahead)
- `fix/tests-seamfix-deepseek` (tip in master, 0 ahead)
- `fix/tests-seamfix-gpt` (tip in master, 0 ahead, same as deepseek)
- `fix/tests-seamfix-mistral` (1 commit ahead, but 2-line fix subsumed in master at `thread-settler.ts:155`)

**Worktree coordination required:** The 4 branches are checked out by other parallel sessions. Coordinate before `git worktree remove --force`. After worktrees are removed, `git branch -D` each.

---

## W12 Estimated Effort

| Ticket | Effort | Risk | Lines affected |
|---|---|---|---|
| T1: parity-attrs delete | 30 min | LOW | -472 |
| T2: webgl-bridge porting | 3 hours | MEDIUM | +310 / -40 |
| T3: lifecycle stubs audit | 1 hour | LOW | -200 to -400 (TBD) |
| T4: window-actions dedup | 1 hour | MEDIUM | -50 to -100 (TBD) |
| T5: event-bus collapse | 30 min | LOW | -22 |
| T6: adapter-deps fix | 15 min | LOW | -1 |
| T7: triggers dedup | 5 min | TRIVIAL | -1 |
| T8: adapters types | 1 hour | LOW | 0 |
| T9: branch cleanup | 15 min | LOW | 0 (just branches) |
| **Total** | **~7.5 hours** | | **~700 LOC net** |

---

## Branch Cleanup (W12-T9 detail)

All 4 fix/* branches are safe to delete. Steps:

```bash
# 1. Coordinate with parallel sessions to clear the 4 worktrees
git worktree remove "C:/Users/HP/Desktop/Temp while my comp is at the shop/semantic-explorer-canvastdz" --force
git worktree remove "C:/Users/HP/Desktop/Temp while my comp is at the shop/seamfix-deepseek" --force
git worktree remove "C:/Users/HP/Desktop/Temp while my comp is at the shop/seamfix-gpt" --force
git worktree remove "C:/Users/HP/Desktop/Temp while my comp is at the shop/seamfix-mistral" --force

# 2. Delete the branches
git branch -D fix/canvas-tdz-nemotron
git branch -D fix/tests-seamfix-deepseek
git branch -D fix/tests-seamfix-gpt
git branch -D fix/tests-seamfix-mistral
```

⚠️ **Sequencing:** Wait for the parallel sessions on those 4 worktrees to finish their work and commit before removing. Or coordinate via steer messages.

---

## What W12 is NOT

Based on the pre-emption sweep, W12 is **not** a porting arc. The porting work is done. W12 is a cleanup arc.

Specifically, W12 is **not**:
- A new Svelte 5 porting wave (the legacy `js/modules/*` is at ~149 .ts files, all stable)
- An engine kernel rewrite (W11-T10 already thinned the render loop)
- A build:legacy retirement wave (W11-T11 retired the legacy build)

W12 **is**:
- Removing redundant code (parity-attrs.ts, no-op stubs, event-bus shims)
- Porting the LAST bridge dependencies (~310 LOC)
- Cleaning up dead branches
- Documenting the W11 closeout for future reference

---

## Doctrine Validation (for future subagent dispatches)

| Metric | Result |
|---|---|
| Subagents in scope | 3/3 |
| Off-seam drift | 0 |
| Cost | ~$0.02 total |
| Wall time | ~7 min |
| Findings quality | 8+ real subsumption items, 0 false positives |
| Models used | mimo-v2.5 (paid, A), deepseek-v4-flash-free (B), nvidia/nemotron-3-super-120b-a12b (C) |

**Two lessons for the next dispatch:**
1. Worker C got into a git archaeology reasoning loop and never wrote its report file. **Fix:** add explicit "once verdict is confirmed for X, do NOT re-verify" to the prompt.
2. Mixed-provider strategy works. Mimo-v2.5 is worth the cost for code analysis (~2-3x faster than free models for this workload).

---

## Files in this Pre-emption Sweep

| Path | Purpose |
|---|---|
| `tmp/w12-preempt-orchestration/INVENTORY.md` | 270 lines, 8 sections — orchestration port inventory |
| `tmp/w12-preempt-journey/INVENTORY.md` | 470+ lines — journey port inventory + subsumption recommendations |
| `tmp/w12-preempt-branches/COMPARISON.md` | 8 sections — fix/* branch subsumption analysis |
| `tmp/w12-preempt-*/ocw_*/stdout.log` | Raw worker transcripts (per-isolate) |
| `docs/w12-charter-2026-06-15.md` | This file |

**W12 worktrees:** all 3 subagent worker subdirs are isolated; safe to keep as evidence or delete after W12 closes.

---

## Next Session Handoff

If W12 continues in a future session, this doc + the 3 INVENTORY/COMPARISON reports are the inputs. The recommended first move:

1. Coordinate with parallel sessions to land their in-flight work (App.svelte, JourneyChrome.svelte, focus-pocket.ts, journey-neighborhood.ts)
2. Then run W12-T9 (branch cleanup) — all 4 worktrees need to be cleared first
3. Then W12-T1 (parity-attrs delete) — small, high-value, low-risk
4. Then W12-T2 (webgl-bridge porting) — biggest, needs WebGL testing
5. Then audit tickets T3-T8 in priority order

If W12 should NOT continue and a different arc should start, the W11 closeout is the natural handoff point: master is clean, tests pass, no critical gaps.
