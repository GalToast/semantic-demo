# Visual QA Round 3 — Post-W11+W13+W14+W15+W16 Closure

**Date:** 2026-06-16
**Worker:** READ-ONLY verification
**Dev server PID:** 3340 (restarted mid-test)
**Duration:** ~25 min

## Test Results

| # | Test | Status | Notes |
|---|------|--------|-------|
| 1 | Initial page load | ✅ PASS | Page loads clean. H1 "Semantic Explorer — Montgomery County Business Network" present. 6 mode chips (Overview, Search, Trail, Focus, Inside, Map) in radiogroup. Banner text: "County-wide overview across all visible records." No console errors on initial load. |
| 2 | 3D canvas pixel sample | ✅ PASS | Canvas exists (1440×900), WebGL 2.0 context active (WebKit WebGL, OpenGL ES 3.0). Zero pixel samples due to `preserveDrawingBuffer: false` (expected). 15 legend items loaded. bodyDataActiveView="galaxy". **Verified via DOM/context/logs** — no initSemanticLens null-ref errors. |
| 3 | URL state sync | ✅ PASS | `?view=map&q=coffee`: URL preserved, search input shows "coffee", bodyDataActiveView="map". `?view=galaxy&q=coffee`: URL preserved, search input shows "coffee", bodyDataActiveView="galaxy". q=coffee persists across view changes. |
| 4 | Global event handlers | ⚠️ PARTIAL | Map mode chip click works (URL changes to `?view=map`, bodyDataActiveView switches to "map"). **Escape does NOT return to overview** — pressing Escape navigated to `about:blank` (browser back-nav intercepted the key). App-level Escape handler may not be bound or is being overridden. |
| 5 | Keyboard help overlay | ❌ FAIL | Pressing '?' did not produce a keyboard help overlay. No `[role=dialog]`, `[role=region][aria-label*=keyboard]`, `.keyboard-help`, or `#keyboard-help` found in DOM. The "Open keyboard shortcuts" button (ref=e15) exists in the header but clicking it via evaluate found zero visible `<button>` elements (DOM timing issue between snapshot and evaluate). **Same failure as Round 2 — the '?' keybinding is not wired.** |
| 6 | Search suggestions | ✅ PASS | Typed "cof" → mode auto-switched to Search (radio [checked]). Banner: "Search results across all business records." Static dev fallback showed "Found 18 spots for cof". No dropdown/listbox (expected for static fallback). PHP 502 from api.php is environmental, not code. |
| 7 | Console error summary | ✅ PASS | **0 initSemanticLens errors.** 0 "Cannot read properties of null" errors. 0 "state.scene is null" errors. 3 errors total: (1) 502 from api.php [environmental], (2-3) two 404s for `camera-controls.ts` Vite HMR module resolution [stale cache reference, non-blocking]. |

## Screenshots

| # | Path | Description |
|---|------|-------------|
| 1 | `01-initial-load.png` | Initial page load — 3D canvas rendering with colorful mycelium nodes, mode chips visible, welcome card showing |
| 2-6 | (skipped) | Screenshot tool was MCP-wedged (5s timeouts) — evidence captured via DOM evaluate + accessibility snapshot instead |

## Console Errors

| # | Error | Source | Severity | Pre-existing? |
|---|-------|--------|----------|---------------|
| 1 | Failed to load resource: 502 (Bad Gateway) | api.php?action=semantic_search | LOW | ✅ Environmental (PHP not running) |
| 2 | Failed to load resource: 404 (Not Found) | camera-controls.ts?t=... | LOW | ✅ Vite HMR stale cache |
| 3 | Failed to load resource: 404 (Not Found) | camera-controls.ts?t=... | LOW | ✅ Vite HMR stale cache (duplicate) |

**No HIGH-severity errors.** Zero initSemanticLens, zero null-ref, zero state.scene errors.

## Visual Anomalies

- **MCP screenshot wedge**: `playwright_browser_take_screenshot` timed out at 5s repeatedly — documented as MCP-side issue, not app regression.
- **Camera-controls 404s**: Vite HMR module resolution for `camera-controls.ts` returns 404 on certain cache paths. Non-blocking; the module loads correctly on initial navigation.

## Cross-Seam Findings (DO NOT FIX)

1. **Escape key navigation regression (Test 4)**: Pressing Escape from Map mode navigates to `about:blank` instead of returning to Overview. The app's global Escape handler may not be bound after the W15 search-state retirement, or the handler is being intercepted by browser default behavior. **File as: `?q=js/modules/event-bindings.js or lifecycle.js — Escape handler wiring lost post-W15`**.
2. **Keyboard help overlay missing (Test 5)**: The '?' keybinding does not produce a keyboard shortcuts overlay. The "Open keyboard shortcuts" button exists in the DOM but the overlay itself doesn't appear. **File as: `?q=keyboard-help-overlay — '?' keybinding not wired after W15 search-state cleanup`**.

## Comparison to Round 2

| # | Test | R2 status | R3 status | Delta |
|---|------|-----------|-----------|-------|
| 1 | Initial load | ✅ | ✅ | none |
| 2 | 3D canvas | ❌ blank | ✅ | **FIXED** — initSemanticLens null-ref resolved; canvas renders |
| 3 | URL state | ⚠️ partial | ✅ | **FIXED** — q=coffee now persists across view changes |
| 4 | Event handlers | ✅ | ⚠️ | **REGRESSION** — Escape now goes to about:blank (was working in R2) |
| 5 | Keyboard help | ❌ | ❌ | **PERSISTS** — '?' keybinding still not wired |
| 6 | Search suggestions | ⚠️ 502 | ✅ | **IMPROVED** — static dev fallback now shows "18 results" |
| 7 | Console errors | 3 errors (1 initSemanticLens) | 3 errors (0 initSemanticLens) | **FIXED** — initSemanticLens null-ref is GONE |

## Summary

**W11+W13+W14 engine port arc is verified functional.** The Round 2 initSemanticLens null-ref is GONE — zero occurrences across all tests. The 3D canvas is rendering (WebGL 2.0 context active, 15 legend items loaded, body data-attrs correct). URL state sync (`?view=map&q=coffee`) now persists q= across view changes (Round 2 partial, now fixed). The static dev fallback correctly shows search results for "cof" despite the PHP 502.

**Two regressions/issues to address before W17 closure:**
1. **Escape key regression**: Pressing Escape from Map mode navigates to `about:blank` instead of returning to Overview. Likely a handler wiring issue from the W15 search-state retirement.
2. **Keyboard help overlay (persistent)**: The '?' keybinding still does not show a keyboard shortcuts overlay. This was a Round 2 failure and remains unfixed.

**1 console error is the PHP 502 environmental, not code.** The two camera-controls.ts 404s are Vite HMR cache artifacts and non-blocking.

**Recommended next move:** Wire the Escape key handler and keyboard help overlay before marking the W11+W13+W14+W15+W16 wave as fully closed.
