# W43 Charter — A11y Closeout + Performance Attack + Parity Smoke

**Date:** 2026-06-18
**Status:** Draft
**Parent:** `docs/bug-thread-inspector-baseline-and-activation-2026-06-18.md`, W42-B a11y sweep (commit `7a5bd26`)
**Supersedes:** Previous W43 draft (commit `6a55bce`, focus-stage QA — deferred to parallel session)

---

## Context

### What shipped since W42

| Commit | What | Impact |
|--------|------|--------|
| `7a5bd26` | W42-B: canvas focus-visible ring, thread-inspector SR labels, search live announcement | 3 of 5 W42-B a11y items landed |
| `b0dc537` | bridge-retirement: 2 bridges flipped to native ports (`focus-pocket-bridge`, `camera-controls-restore`) | 2 of 41 bridge files retired |
| `f0f3410` | Thread-inspector: `pinFirstAvailableNeighbor`, empty-state guidance, unskip baseline | W42-A complete |
| `cca2d79` | W42 charter marked complete | — |
| `7dd42b6` | Lighthouse performance gate script | CI perf gate live |

### Remaining W42-B a11y items (not yet landed)

| Item | Status | Commit needed |
|------|--------|---------------|
| Keyboard trap in search results (arrow keys, Esc) | **NOT landed** — `SearchResults.svelte` has no keyboard handlers | TBD |
| Color contrast audit of secondary text in focus stage | **NOT landed** — no contrast fixes in `7a5bd26` diff | TBD |

### Performance reality (Lighthouse baseline `docs/lighthouse-baseline-2026-06-18.json`)

| Metric | Actual | Budget | Status |
|--------|--------|--------|--------|
| LCP | **17.1 s** | < 2.5 s | 🔴 6.8× over |
| TBT | **1,790 ms** | < 200 ms | 🔴 9× over |
| CLS | 0.011 | < 0.1 | 🟢 OK |
| Speed Index | 3.3 s | — | 🟡 borderline |
| FCP | 1.4 s | — | 🟡 borderline |
| Performance score | **0/100** | — | 🔴 |

Root cause per `bootup-time` audit: `index-DAW2pTKT.js` takes 3,573 ms CPU time (3,528 ms scripting). The main bundle is 585 KB raw — this is the first-load execution cost of the non-lazy-loaded app shell.

### Bundle status (W40 audit)

| Metric | Actual | Ceiling | Status |
|--------|--------|---------|--------|
| JS raw | 2,539 KB | 2,500 KB | 🔴 1.6% over ceiling |
| JS gzip | 610 KB | 650 KB | 🟢 OK |
| CSS raw | 54.6 KB | 60 KB | 🟢 OK |
| CSS gzip | 9.7 KB | 12 KB | 🟢 OK |

The W41 THREE.js selective import conversion (`fc0c4bc`) should have reduced the 1,217 KB Three.js module — verify current state post-conversion.

### Bridge surface

41 bridge files remain in `src/lib/engine/*-bridge.ts`. Commit `b0dc537` retired 2 (39 remain). A load-bearing analysis report does **not exist** in `docs/` — marked TBD below.

---

## Goals

### W43-A: Remaining W42-B A11y Items

**Impact:** WCAG 2.1 AA compliance — search keyboard navigation is a critical user flow
**Scope:**

1. **Keyboard trap fix in `SearchResults.svelte`**: Add arrow-key navigation (Up/Down to move through results, Enter to select, Escape to close search panel)
2. **Color contrast audit**: Run axe-core against focus-stage surfaces (`focus-search`, `focus-programmatic` states) and fix any secondary-text contrast failures below 4.5:1
3. **A11y baseline re-run**: Capture violation counts for all 4 states (currently TBD in `docs/a11y-baseline-2026-06-18.md`)
4. **Contract test additions**: Extend `tests/unit-active/a11y-w42b-contract.test.mjs` with keyboard-navigation assertions

**Files:** `src/components/SearchResults.svelte`, `src/components/InfoPanel.svelte`, `docs/a11y-baseline-2026-06-18.md`, `tests/unit-active/a11y-w42b-contract.test.mjs`
**Risk:** MED — keyboard handlers can conflict with global key bindings; contrast fixes may alter visual design tokens
**Verification:** `npm run test:a11y` passes with ≤ 5 violations per state; keyboard nav test assertions pass

---

### W43-B: Performance Budget Adherence

**Impact:** LCP 17.1 s and TBT 1,790 ms are unacceptable for production; bundle is 1.6% over JS ceiling
**Scope:**

1. **Verify post-THREE.js-conversion bundle size**: Run `npm run build` + `node scripts/check-bundle-size.mjs` to confirm the selective-import conversion in `fc0c4bc` actually reduced the Three.js chunk
2. **Identify TBT root causes**: Profile `index-DAW2pTKT.js` (3,528 ms scripting) to determine if it's initialization, Svelte hydration, or Three.js setup
3. **Draft W44 performance attack plan**: Based on profiling, identify the top 3 optimization levers (e.g., deferred Three.js initialization, web worker offload, further lazy-loading)
4. **Verify Lighthouse gate script works**: Run `node scripts/lighthouse-gate.mjs` against the production preview build to confirm CI gate catches regressions

**Files:** `scripts/lighthouse-gate.mjs`, `scripts/check-bundle-size.mjs`, `docs/performance-budget.md`, `docs/lighthouse-baseline-2026-06-18.json`
**Risk:** LOW — read-only profiling and documentation; no source edits needed for this charter
**Verification:** Updated bundle-size numbers in perf budget doc; W44 attack plan drafted

---

### W43-C: Production-Preview Parity Smoke + Visual Contract Refresh

**Impact:** Parallel session is modifying focus-stage rendering (M-flagged: `stage-renderer.ts`, `focus-stage-renderer.ts`, `compass-controller.ts`, `InfoPanel.svelte`, `ModeChips.svelte`, `Header.svelte`, `LegacyCompassSurface.svelte`) — these need visual baseline refresh once they land
**Scope:**

1. **Parity-attrs smoke**: Verify all `document.body.dataset.*` attributes are correctly set across mode transitions (galaxy → search → focus → journey). The race in `panelSurfaceMode` was fixed in `a833d6e` — confirm it holds
2. **Body-data-attr audit**: Run the body-attr contract tests to confirm no regressions from parallel session work
3. **Visual baseline refresh** (post parallel-session): Once parallel session commits, regenerate visual baselines for all affected surfaces using the visual regression test script
4. **Parity-attrs final closeout**: If any remaining parity-attrs gaps exist (per `docs/production-preview-parity-baseline-2026-06-17.md` — TBD — verify path), document and fix

**Files:** `tests/unit-active/` (body-attr contracts), `tests/visual-regression.test.ts`, `docs/production-preview-parity-baseline-2026-06-17.md` (TBD — verify path exists)
**Risk:** MED — visual baselines must be regenerated after parallel session lands; regenerating too early creates stale baselines
**Verification:** All body-attr contract tests pass; visual regression passes against refreshed baselines

---

## Coordination

### Safe zones (this charter owns)

- `docs/` — charter, baselines, attack plans
- `tests/unit-active/a11y-w42b-contract.test.mjs` — keyboard nav assertions
- `tests/unit-active/` — body-attr contracts (read + verify, not modify source)

### Do NOT touch (high-risk surfaces from AGENTS.md parallel session)

| File | Owner | Why |
|------|-------|-----|
| `src/components/Header.svelte` | Parallel session | M-flagged, focus-stage rendering |
| `src/components/InfoPanel.svelte` | Parallel session | M-flagged, focus-stage rendering |
| `src/components/LegacyCompassSurface.svelte` | Parallel session | M-flagged, compass surface |
| `src/components/ModeChips.svelte` | Parallel session | M-flagged, mode switching |
| `src/lib/focus/stage-renderer.ts` | Parallel session | M-flagged, core rendering |
| `src/lib/journey/focus-stage-renderer.ts` | Parallel session | M-flagged, journey rendering |
| `src/lib/orchestration/compass-controller.ts` | Parallel session | M-flagged, compass orchestration |
| `src/lib/state/app.svelte.ts` | Parallel session | M-flagged, global state |
| `src/lib/stores/focus.svelte.ts` | Parallel session | M-flagged, focus store |
| `src/lib/types/state.ts` | Parallel session | M-flagged, type definitions |

### Subagent-ready seams

| Task | Estimated effort | Worker count | Prerequisite |
|------|-----------------|--------------|--------------|
| W43-A: keyboard trap fix | 30 min | 1 | None — SearchResults.svelte is NOT M-flagged |
| W43-A: contrast audit + fixes | 45 min | 1 | None — can run axe-core independently |
| W43-B: bundle size verification | 10 min | 1 | None — read-only |
| W43-B: TBT profiling | 20 min | 1 | None — read-only |
| W43-C: parity-attrs smoke | 15 min | 1 | None — read-only |
| W43-C: visual baseline refresh | 30 min | 1 | **Blocked on parallel session landing** |

---

## Success Criteria

### W43-A: A11y Closeout
- [ ] `SearchResults.svelte` supports arrow-key navigation (Up/Down/Enter/Escape)
- [ ] `SearchResults.svelte` keyboard nav has contract test coverage
- [ ] axe-core scan shows ≤ 5 violations for all 4 a11y baseline states
- [ ] Color contrast of secondary text in focus stage meets 4.5:1 ratio
- [ ] `docs/a11y-baseline-2026-06-18.md` updated with actual violation counts (no more TBD)

### W43-B: Performance
- [ ] Bundle size verified post-THREE.js-conversion (confirm ≤ 2,500 KB raw ceiling)
- [ ] TBT root cause documented (which codepath dominates the 3,528 ms scripting time)
- [ ] W44 performance attack plan drafted (top 3 levers with expected savings)
- [ ] Lighthouse gate script passes against production preview build
- [ ] `docs/performance-budget.md` updated with current actuals

### W43-C: Parity + Visual Contracts
- [ ] Body-data-attr parity contract tests pass (all mode transitions)
- [ ] `panelSurfaceMode` race fix from `a833d6e` verified stable
- [ ] Visual baselines regenerated for focus-stage surfaces (post parallel-session)
- [ ] No parity-attrs gaps remain (or documented as known/deferred)

### Cross-cutting
- [ ] All existing tests still green (`npm run test:unit`)
- [ ] `svelte-check` 0 errors, 0 warnings
- [ ] No M-flagged source files modified
- [ ] Committed and pushed

---

## Deferral List

| Item | Reason | Target wave |
|------|--------|-------------|
| Bridge retirement (39 remaining) | Out of scope for W43 — needs load-bearing analysis first | W44+ (blocked on bridge-load-bearing report — TBD) |
| Bridge load-bearing report | Does not exist in `docs/` — must be authored before bulk retirement | W44 |
| `../js/scanner.js` deploy script decoupling | No `../js/scanner.js` found in filesystem — may be stale reference | TBD — verify path |
| Deploy script `../js/scanner.js` decoupling | Same as above | TBD |
| Production-preview parity baseline doc | Path `docs/production-preview-parity-baseline-2026-06-17.md` not verified — may not exist | TBD — verify path |

---

## Docs Contradictions Found

1. **W41 charter (commit `b562f2c`)** marks T41-B as `[x]` complete with commit `80e4224`, but the checkbox for "All tests green" is unchecked. The status line says "Tests green (93/93, 1068/1068)" — contradictory.
2. **W40 charter** references `docs/w40-bundle-audit-2026-06-18.md` as parent, but the actual file is `docs/w40-bundle-audit-2026-06-18.md` — path matches. However, W40 charter says "W39 completed" in its status, implying W40 is about verification, not new work. This is fine.
3. **Performance budget** lists "Current" JS raw as 2,539 KB, but the W40 audit says the same number. The W41 selective-import conversion (`fc0c4bc`) should have reduced this. Budget doc may be stale — flagged for W43-B verification.

---

## Parent Doc Pointer

- W42-bug doc: `docs/bug-thread-inspector-baseline-and-activation-2026-06-18.md`
- W42 charter: `docs/w42-charter-2026-06-18.md` (W42-A ✅, W42-B ✅ with 3/5 items landed)
- A11y baseline: `docs/a11y-baseline-2026-06-18.md` (violation counts TBD)
- Lighthouse baseline: `docs/lighthouse-baseline-2026-06-18.json`
- Performance budget: `docs/performance-budget.md`
