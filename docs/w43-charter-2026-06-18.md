# W43 Charter — Focus Stage QA + Performance Prep

**Date:** 2026-06-18
**Status:** Drafted while parallel session is in-flight
**Parent:** Parallel-session focus-stage rendering work (uncommitted)

---

## Goals

### W43-A: Focus Stage QA Sweep

**Impact:** The parallel session has been modifying focus-stage rendering surfaces all day. Once it lands, we need a focused QA pass to catch visual regressions before moving on.

**Scope:**

1. Wait for parallel session to commit focus-stage rendering work
2. Run full visual regression suite on focus-stage surfaces
3. Fix any regressions in focus stage, info panel, or compass controller
4. Add contract tests for any new rendering surface
5. Document the focus-stage rendering architecture if needed

**Files:** `src/lib/focus/stage-renderer.ts`, `src/lib/journey/focus-stage-renderer.ts`, `src/components/InfoPanel.svelte`, `src/components/LegacyCompassSurface.svelte`

### W43-B: Performance Prep

**Impact:** Lighthouse score is 33/100 with TBT 1,790ms and LCP 17.1s. Before attacking performance, we need a stable rendering baseline.

**Scope:**

1. Verify current Lighthouse baseline after focus-stage rendering lands
2. Identify whether the new rendering work improves or worsens the score
3. Draft a performance attack plan for W44

---

## Coordination

1. **Do not touch** focus-stage files while parallel session is active
2. **Do not commit** on top of parallel session WIP
3. Once parallel session lands, run:
   - `npm run test:unit`
   - `npm run qa:surface:all`
   - `npm run build`
   - `npm run check`

## Success Criteria

- [ ] Parallel session focus-stage work committed and reviewed
- [ ] Visual regression passes on focus-stage surfaces
- [ ] Contract tests added for new rendering surface
- [ ] Lighthouse baseline re-run and documented
- [ ] W43-A and W43-B completed, or W44 performance charter drafted
