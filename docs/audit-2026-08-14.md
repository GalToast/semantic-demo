# Audit 2026-08-14 — swarm wave 1+2 consolidation

Source lanes: 8 external-subagent dispatches (poolside/laguna-s-2.1 warm; opencode-zen hit ZenMux cooldown 429 — infra, not skill). Reports under `tmp/swarm-audit/`.

## Findings (each verified, receipts on disk)

| Area                    | State                                                                                                                                                                                                                                                                        | Evidence / follow-up                                                                                                                                                   |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Worker payload boundary | ✅ FIXED + committed (`2a2c4a69`)                                                                                                                                                                                                                                            | 12/12 unit green (data-worker-payload-validation.test.ts)                                                                                                              |
| Nav-surface refactor    | ✅ lockstep parity healthy (12-predicate twins; `focus-gate-lockstep-contract.test.ts` 2/2)                                                                                                                                                                                  | `parity-report.md`; post-merge: gates⇄`surface-mode-map` invariant test + `CompassRail:164` use parity `focusSearchForced` (patch drafted: `parity-followup-patch.md`) |
| Micro-demo engine       | deprecated `src/lib/engine/demo-choreography.ts` (576 LOC), ONE live caller (`lifecycle.ts:652 → clearDemoTimers`), 9/10 exports dead re-exports                                                                                                                             | sweep executor (sa-sweep) in flight; plan: `demo-migration-plan.md`; AGENTS.md de-rotted (true story)                                                                  |
| Dev-tooling shipping    | already DEV-gated in App.svelte (`import.meta.env.DEV` guard, W48-T1)                                                                                                                                                                                                        | no work needed                                                                                                                                                         |
| Release hardening       | Lighthouse tooling EXISTS (`qa-lighthouse.mjs`, `lighthouse-gate.mjs`, 6 rechecks) but baseline never re-committed post-W44; visual-state registry = **27** IDs (docs say 26 — fixed in migration-plan); dual-URL normalization = **product decision**, mislabeled as defect | new `scripts/qa-lighthouse-baseline.mjs` + runbook (build+serve needed to refresh; `dist/` awaits calm lane window)                                                    |

## Doc corrections applied this session

- `AGENTS.md`: micro-demo paragraph rewritten to the post-migration truth (engine file, live-caller, swipe path).
- `docs/migration-plan.md`: visual-registry count 26 → **27**.

## Resolved limbo

Wave-5 commits landed the once-deferred queue items, closing the in-flight gap:

- **A** micro-demo retirement -> **LANDED `3f3a592c`**; sa-sweep reviewed, contract tests rewritten to assert-absence, reversion-guard OK.
- **B** lighthouse ops runner -> **LANDED `aea7ded2`**; `scripts/qa-lighthouse-baseline.mjs` + suggested script key committed.
- **C** parity follow-up -> surface-mode-map (`01b2b8eb`) landed first, then `CompassRail:164` getter (`00dbc67c`) + gates-map invariant pin (`85c71684`).
- **dual-URL decision** -> resolved as 308 normalizer (`5841957a`), not a migration defect.

## Wave-5 finale

Final commit list (verified `git log --oneline -12`, newest-first):

| Hash       | Summary                                                                               |
| ---------- | ------------------------------------------------------------------------------------- |
| `5841957a` | chore(deploy): normalize dual-URL — 308 redirect + test repoint                       |
| `aea7ded2` | chore(perf): add repeatable Lighthouse baseline runner                                |
| `85c71684` | test(nav): pin focus gates to canonical surface-mode-map                              |
| `75244158` | refactor(demo): drop dead ui.ts module + stale getFirstSearchHit re-export            |
| `8805efac` | chore(tests): sync contract manifests after demo sweep (minimal diff)                 |
| `00dbc67c` | refactor(components): CompassRail use parity focusSearchForced getter                 |
| `3f3a592c` | refactor(engine): retire micro-demo choreography engine (576 LOC)                     |
| `01b2b8eb` | feat(nav): surface-mode canonical map — single source of truth for surface->mode/view |

### Remaining queue

- **deploy single-publish** — deferred (lane).
- **camera-guards** — in flight (d1).
- **orphans** — triage done.
- **B npm-key** — parked.

### Operator note

`.htaccess` 308 ships with the next deploy; no per-commit artifact.

## Perf campaign close (2026-08-15)

- **Instruments:** baselines `8796e95c` (mobile perf 29 / LCP 11.7s; desktop 38 / 5.1s) +
  CI gate `4f162018` (`node scripts/qa-lighthouse-gate.mjs`; thresholds perf≥-5/TBT≤+100ms/LCP≤+300ms).
- **Diagnosis (p1-p3 swarms + main-lane bundle audit):** mobile LCP is NETWORK-BYTES bound
  (LCP=text; canvas gated off bare `/`; 0 long tasks; unthrottled FCP 540ms). Elephant =
  `mode-transition-deps` 1,240KB — orchestration closure; documented as deferred lazification.
- **Shipped:** `.htaccess` precompressed gz/br twins `bea7f839` (br→~340-420KB wire est.);
  lazification plan `docs/perf-lazify-plan.md` w/ w1 seam inventory (app-init:31 rIC-undermine;
  url-state isDeepLink gate); awaiting orchestration-lane commit.
