Vision capability probe (worker native-read path — regression check after pi-model-providers harness fix).

Use the `read` tool on exactly this file:
  /c/Users/HP/repos/semantic-explorer/tmp/vision-jury/11-postfix-card.png

This file is a 260x545 close-up of a dark-themed business card.

Reply ONE line starting exactly with:
- "PIXELS OK — " followed by one sentence describing what you saw (colors, any text you can read, button names).
- OR "VISION UNAVAILABLE — model is text-only" if the read result says images are omitted / unsupported.

Rules:
- Use ONLY the read tool. No bash, no other tools.
- Do not guess from the filename. The filename is deliberately meaningless.
- Keep your reply under 3 sentences.