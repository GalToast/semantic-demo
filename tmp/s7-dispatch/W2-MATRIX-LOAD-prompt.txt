WORKER S7-W2 — MATRIX-LOAD (opencode-key-router.mjs Site A + Site B)
==================================================

# MISSION

Load the full 8-entry V2 overlay matrix from
`C:/Users/HP/repos/semantic-explorer/tmp/v2-overlay-matrix.json` into
the `v2ModelMatrix` const at BOTH Site A (~line 3575) and Site B
(~line 4189) of
`C:/Users/HP/harness/servers/key-router/src/opencode-key-router.mjs`.

Currently both V2 integration blocks have a minimal 1-entry
matrix inline (just `agnes-2.0-flash` — same URL as the primary
carrier). After the matrix-load, V2 dispatch can actually find
an ALTERNATIVE carrier for a failed primary and deliver the V2
SUCCESS path.

# CWD

  C:/Users/HP/harness/servers/key-router

# ALLOWED FILES (worker MAY edit these ONLY)

  - C:/Users/HP/harness/servers/key-router/src/opencode-key-router.mjs
  - C:/Users/HP/harness/servers/key-router/src/v2-overlay-matrix.json (NEW —
    you will copy from tmp/ to here for relative-import convenience)

# NO-REVERT BOUNDARIES (worker MAY NOT touch)

  - v2-failover-overlay.mjs (W1 owns it — don't touch)
  - Don't restart the key-router — main-lane coordinates the restart
    after BOTH W1 + W2 patches land together.

# PROCEDURE

  1. Save backup BEFORE patching:
     `cp src/opencode-key-router.mjs src/opencode-key-router.mjs.bak-pre-matrix-load-2026-07-26`

  2. Copy the matrix JSON from semantic-explorer tmp/ to harness src/:
     `cp C:/Users/HP/repos/semantic-explorer/tmp/v2-overlay-matrix.json src/v2-overlay-matrix.json`
     (this makes a relative-path import / readFileSync feasible from
     inside the running router)

  3. READ the existing v2ModelMatrix const at Site A (~line 3575):
     - It's inside the `if (v2Flag === '1') { try { ... } }` block in
       the no-active-keys backoff route.
     - It currently looks like `const v2ModelMatrix = [{ modelId: 'agnes-2.0-flash', ... }];`
     - Replace with:
       `const v2ModelMatrix = JSON.parse(fs.readFileSync(path.join(__dirname, 'v2-overlay-matrix.json'), 'utf8'));`
       (use fs + path — both are already imported at top of file, verify
       with `grep -nE "from .node:fs|from .node:path" src/opencode-key-router.mjs`)
     - If `__dirname` isn't available in ESM context, use
       `import.meta.url` resolution pattern (fileURLToPath + dirname). Verify
       by reading the existing top-of-file: if `__dirname` is already
       defined via a helper, use it.

  4. READ the v2ModelMatrix const at Site B (~line 4189) and apply the
     SAME replacement.

  5. Verify:
     - `node --check src/opencode-key-router.mjs` must EXACTLY exit 0
     - `grep -nE 'v2ModelMatrix' src/opencode-key-router.mjs` shows exactly
       3 lines: line ~3575 (Site A), line ~4189 (Site B), and any usage of
       v2ModelMatrix as argument to v2FailoverDispatch call
     - `ls -la src/v2-overlay-matrix.json` shows 4725 bytes

# REQUIRED EVIDENCE (write to this file)

  C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/matrix-load-report.md

  Report contents required:
    - Before-patch code excerpt from Site A (lines ~3565-3580 area,
      showing the v2ModelMatrix declaration + surrounding if(v2Flag) block)
    - Before-patch code excerpt from Site B (same area around line 4180-4196)
    - After-patch code excerpt from Site A + Site B (the JSON.parse pattern)
    - line numbers from `grep -nE 'v2ModelMatrix' src/opencode-key-router.mjs`
    - node --check exit code + output
    - Backup file size confirmation
    - One-paragraph summary suitable for bench-log
    - Note: ARE there any routes in the 8-entry matrix that you expect to
      work end-to-end? (e.g. openrouter/north-mini-code:free could serve
      as fallback for /zydit/v4 — flag any routes you expect to fail)

# OPTIONAL BONUS (only if ahead of time)

  Add a startup log message on router boot:
  `[v2-overlay] Loaded ${v2ModelMatrix.length} matrix entries from
  v2-overlay-matrix.json` to confirm loading worked.

# MODEL

  Default `pi:router-opencode-zen/mimo-v2.5-free` or `router-agnes` lane
  (free, $0). Reasoning max (HARDCODED default for subagents — don't
  downgrade based on payload size, rambling, or stdout flooding).

# TIMEOUT

  30 min (1800s) standard.

# COST BUDGET

  $0 expected.

# POST-RUN HUMAN-CHECKABLE ARTIFACTS

  - `src/opencode-key-router.mjs.bak-pre-matrix-load-2026-07-26`
  - `src/opencode-key-router.mjs` (patched)
  - `src/v2-overlay-matrix.json` (new file, copied from tmp/)
  - `tmp/s7-dispatch/matrix-load-report.md`

Main-lane will:
  - read the report + verify self-consistency with file:line cites
  - run node --check independently on the patched file
  - coordinate restart with W1 worker's patch (single restart from main-lane)
  - smoke-test matrix load works at boot
  - re-run W4's e2e test live post-restart
