# W6 — V2 Success-Path E2E Test Runner

## Task
Run the V2 success-path e2e test script against the live key-router and capture results.

## Script
`C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/v2-success-path-e2e.mjs`

## Prerequisites (verified by main-lane)
- W1 slotHandle fix: APPLIED (3 occurrences in v2-failover-overlay.mjs)
- W2 matrix-load: APPLIED (4 occurrences in opencode-key-router.mjs)
- Key-router: RUNNING (PID 23920, port 8788, HTTP 200)
- E2e script: SYNTAX-VALID (561 lines, node --check passed)

## What the script does
1. Sends 6 POST requests to `http://127.0.0.1:8788` with `X-V2-Failover: 1` header
2. Tests the V2 failover overlay SUCCESS path (alternative-carrier dispatch returning HTTP 200)
3. Appends JSONL results to `tmp/s7-dispatch/v2-success-path-results.jsonl`
4. Prints summary (total/passed/partial/failed)

## Important Notes
- The script documents that V2 triggers when: (A) provider has keys configured AND (B) all active keys exhausted OR all attempts returned FAILOVER_STATUS
- The key-router was NOT started with disabled-slot env vars (`OPENCODE_KEY_ROUTER_DISABLED_SLOTS_*`), so V2 may not trigger reliably for all 6 test cases
- If V2 doesn't trigger, report which tests passed/failed and WHY (missing disabled-slot env vars)
- The script has a "FAIL-MODE" section: if patches aren't landed, it targets expected end-state

## Deliverables
1. Run: `node tmp/s7-dispatch/v2-success-path-e2e.mjs`
2. Capture stdout + JSONL output
3. Write summary to `tmp/s7-dispatch/W6-e2e-run-report.md`
4. Include: total/passed/partial/failed + any V2 trigger issues + recommendations

## Lane
logfare/kiro-auto (Gemini-backed reasoning model, $0.00 per dispatch)
