WORKER S7-W1 — SLOT-HANDLE-FIX
==================================================

# MISSION

Patch a ReferenceError bug in `C:/Users/HP/harness/servers/key-router/src/v2-failover-overlay.mjs`:

Lines ~900 + ~941 reference `slotHandle` but it's never declared. Every V2
failover dispatch call silently throws `ReferenceError` before reaching the
alternative-carrier dispatch loop, gets caught by the V2 block's try/catch
in opencode-key-router.mjs, and falls through to V1 — so the V2 SUCCESS
path (delivering an alternative-carrier HTTP 200) has never actually fired.

# CWD

  C:/Users/HP/harness/servers/key-router

The `src/` dir resolves relative — both v2-failover-overlay.mjs and
opencode-key-router.mjs live there.

# ALLOWED FILES (worker MAY edit these ONLY)

  - C:/Users/HP/harness/servers/key-router/src/v2-failover-overlay.mjs

# NO-REVERT BOUNDARIES (worker MAY NOT touch)

  - Any other file (especially opencode-key-router.mjs — W2 owns it)
  - Don't restart the key-router — main-lane coordinates the restart
    after BOTH W1 + W2 patches land together.

# PROCEDURE

  1. Save backup BEFORE patching:
     `cp src/v2-failover-overlay.mjs src/v2-failover-overlay.mjs.bak-pre-slot-handle-fix-2026-07-26`

  2. READ lines 800-950 of `src/v2-failover-overlay.mjs` to understand the
     for-loop structure and the scope where `slotHandle` should live.

  3. Find `keySlotAcquireFn` usage in the same file. The bug per Sprint-6
     W4 worker's diagnosis: a closure-call `ksAcquire?.(routeId, modelId)`
     or `keySlotAcquireFn(...)` result SHOULD be assigned to slotHandle
     but the variable was never declared.

  4. Reference lines ~900 + ~941 still use the bare `slotHandle`.

  5. Apply the FIX:
     - Declare `let slotHandle = null;` at the top of the for-loop scope
       (inside `for (let i = 0; i < chain.length; i++) {` body), BEFORE
       the line that reads slotHandle.
     - Assign slotHandle inside the loop body using the
       `keySlotAcquireFn` argument (passed in to v2FailoverDispatch) —
       likely something like `slotHandle = keySlotAcquireFn?.(routeId, modelId) ?? null;`.
       Read the surrounding code to find the exact variable names — there
       may be a shorthand alias for keySlotAcquireFn already inside the file.

  6. Verify:
     - `node --check src/v2-failover-overlay.mjs` must EXACTLY exit 0
     - `grep -nE 'slotHandle' src/v2-failover-overlay.mjs` should show the
       declaration + the two uses at ~lines 900 + ~941, exactly once
       declared (at the right scope, confirmed by indent).

# REQUIRED EVIDENCE (write to this file)

  C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/slot-handle-fix-report.md

  Report contents required:
    - Before-patch code excerpt (lines ~885-905 + lines ~935-945 so
      we can see the bare-scope loop and both slotHandle references)
    - Exact line numbers + scope where you declared slotHandle
    - Exact assignment code (what value populates the var)
    - line numbers after patch from `grep -nE 'slotHandle'`
    - node --check exit code + output
    - Backup file size confirmation (`ls -la src/...bak-pre-slot-handle...`)
    - One-paragraph "human-readable summary" suitable for bench-log

# OPTIONAL BONUS (only if ahead of time)

  Write a tiny direct-unit test in
  `C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/v2-overlay-direct-success-test.mjs`
  that imports `v2FailoverDispatch` and exercises the success path with a
  mock-upstream fetch stub returning HTTP 200. Assert: dispatch returns
  `{ success: true, status: 200, ... }` without throwing.

# MODEL

  Default `pi:router-opencode-zen/mimo-v2.5-free` or `router-agnes` lane
  (free carrier, ~$0 cost). Reasoning max (this is a Sprint-7 subagent:
  HIGHEST reasoning tier is hardcoded default — don't gate or downgrade
  based on payload size, rambling, or stdout flooding).

# TIMEOUT

  30 min (1800s) standard.

# COST BUDGET

  $0 expected. Stop immediately if you see logfare or any paid route
  being used (paid routes have $cost > 0 in API response).

# POST-RUN HUMAN-CHECKABLE ARTIFACTS

  - `src/v2-failover-overlay.mjs.bak-pre-slot-handle-fix-2026-07-26`
  - `src/v2-failover-overlay.mjs` (patched)
  - `tmp/s7-dispatch/slot-handle-fix-report.md`
  - Optional: `tmp/s7-dispatch/v2-overlay-direct-success-test.mjs`

Main-lane will:
  - read the report
  - re-verify the patch live (curl + node --check)
  - coordinate restart with W2 worker's patch
  - run W4's e2e test live post-restart
