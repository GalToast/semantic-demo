# Worker W-A: WELFARE-PROVIDERS-PATCH-WRITER

# SCOPE

You are creating the patch file to register 9 new free-LLM welfare providers
into the Pi key router at `C:/Users/HP/harness/servers/key-router/src/opencode-key-router.mjs`.
You will NOT modify the router file directly — you write a git-diff-style patch
file to disk + a signup-guide README, and main-lane (the user's primary chat)
will apply the patch + coordinate the key-router restart.

# REQUIREMENTS

1. READ `C:/Users/HP/harness/servers/key-router/src/opencode-key-router.mjs`
   lines 80-250 to learn the existing provider registration pattern. Each
   provider object has keys: `label`, `routePrefix`, `upstreamBase`, `authHeader`,
   `getKeys`. The `getKeys` calls `loadProviderKeys({ envBase, tokenRegex,
   configFile, [authAlias], [opencodeAlias], [skipProcessEnv] })`.

2. WRITE a unified git-diff style patch file to:
     `C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/welfare-providers-ADD-patch.diff`
   The patch should ADD 9 new provider entries into the `PROVIDERS` const
   object + ADD 9 new token-regex helper functions directly above the PROVIDERS
   const, matching the existing pattern (e.g., `isNvidiaToken`, `isMistralToken`).

3. WRITE a separate signup-guide README to:
     `C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/welfare-providers-signup-guide.md`
   For each provider: signup URL, free tier rate limits (RPM / TPM / RPD),
   free model list, OpenAI-compat base URL, auth scheme, API key token format
   / regex, CC required yes/no, plus a one-line "best use case" recommendation.

# THE 9 WELFARE PROVIDERS (data baked in for you — DO NOT need to websearch)

| # | env var | routePrefix | upstreamBase | auth | token regex / fmt | CC | Free Tier Notes |
|---|---|---|---|---|---|---|---|
| 1 | GROQ_API_KEY | `/groq/v1` | `https://api.groq.com/openai/v1` | Bearer | `gsk_[0-9A-Za-z]{40,80}` | NO | 30 RPM, 30K TPM, 14K RPD Llama 3.3 70B + Llama 4 Scout. Best subagent lane. |
| 2 | DEEPSEEK_API_KEY | `/deepseek/v1` | `https://api.deepseek.com/v1` | Bearer | `sk-[0-9a-z]{32}` | YES | $5 free signup credit + DeepSeek-V3 free model. Excellent coder, weak reasoning. |
| 3 | SILICONFLOW_API_KEY | `/siliconflow/v1` | `https://api.siliconflow.cn/v1` | Bearer | `sk-[0-9A-Za-z]{32,80}` | NO | Free Qwen2.5-72B-Instruct + DeepSeek-V2.5 + GLM-4-Flash. China-based. |
| 4 | TOGETHER_API_KEY | `/together/v1` | `https://api.together.xyz/v1` | Bearer | `[0-9a-f]{40,64}` (hex) | YES | $5 free signup credit. Many Llama + Qwen + Mistral + DeepSeek models. |
| 5 | CEREBRAS_API_KEY | `/cerebras/v1` | `https://api.cerebras.ai/v1` | Bearer | `csk-[0-9a-z]{20,40}` | NO (was beta invite-only) | Super fast Llama 3.3 70B + 405B; usually wide-open beta today. |
| 6 | COHERE_API_KEY | `/cohere/v1` | `https://api.cohere.ai/v1` | Bearer | `[A-Za-z0-9]{40,64}` | YES | Free trial API key w/ 1000s of generations. Command R / Command R+. |
| 7 | HYPERBOLIC_API_KEY | `/hyperbolic/v1` | `https://api.hyperbolic.xyz/v1` | Bearer | `sk-[A-Za-z0-9]{20,40}` | YES | $20 free signup credit. Hot for long-context Llama + Qwen + DeepSeek. |
| 8 | NOVITA_API_KEY | `/novita/v1` | `https://api.novita.ai/v3/openai` | Bearer | `sk-nnt_[A-Za-z0-9]{32}` | NO | Free trial credit. Qwen2.5-72B + Llama 3.3 70B. |
| 9 | AIMLAPI_API_KEY | `/aimlapi/v1` | `https://api.aimlapi.com/v1` | Bearer | `[A-Za-z0-9]{32,80}` | NO | Free 50 req/day. 80+ models gated through one API. |

# TOKEN REGEX HELPER TEMPLATE (write all 9 above the PROVIDERS const)

```js
const isGroqToken = /^(gsk_[0-9A-Za-z]{40,80})$/;
const isDeepSeekToken = /^(sk-[0-9a-z]{32})$/;
const isSiliconFlowToken = /^(sk-[0-9A-Za-z]{32,80})$/;
const isTogetherToken = /^([0-9a-f]{40,64})$/;
const isCerebrasToken = /^(csk-[0-9a-z]{20,40})$/;
const isCohereToken = /^([A-Za-z0-9]{40,64})$/;
const isHyperbolicToken = /^(sk-[A-Za-z0-9]{20,40})$/;
const isNovitaToken = /^(sk-nnt_[A-Za-z0-9]{32,})$/;
const isAimlApiToken = /^([A-Za-z0-9]{32,80})$/;
```

# PROVIDER ENTRY TEMPLATE (write all 9 inside the PROVIDERS const)

```js
groq: {
    label: "Groq",
    routePrefix: "/groq/v1",
    upstreamBase: "https://api.groq.com/openai/v1",
    authHeader: "Bearer",
    getKeys: () =>
        loadProviderKeys({
            envBase: "GROQ_API_KEY",
            tokenRegex: isGroqToken,
            configFile: "groq-keys.json",
        }),
},
```

Mirror that template for every provider. Use the env base / regex / config
file names from the table above.

# SIGNUP-GUIDE README CONTENT (per provider block must include)

For each provider, produce:
- **Heading**: `## Provider Name`
- **Signup URL**: link to user-facing console
- **Free tier table**: RPM, TPM, RPD, free $ credits
- **Free model list**: bullet list of standout free-tier models
- **CC required?**: yes/no
- **API key how-to**: where in the console to find/copy the key
- **Best use case**: 1-line recommendation (e.g., "Best subagent lane for sub-30s tool calls")

Sources you may quote (from main-lane's precompiled websearch today):
- https://klymentiev.com/blog/free-llm-api (13 providers compared)
- https://github.com/mnfst/awesome-free-llm-apis (community list)
- https://console.groq.com/docs/rate-limits
- https://api.together.ai/
- https://deepseek.com/pricing
- https://docs.cohere.com/docs/rate-limits
- https://hyperbolic.xyz/
- https://siliconflow.cn/
- https://novita.ai/
- https://aimlapi.com/api

# CONSTRAINTS

- Touch ONLY these files in this repo:
  - `tmp/s7-dispatch/welfare-providers-ADD-patch.diff` (CREATE)
  - `tmp/s7-dispatch/welfare-providers-signup-guide.md` (CREATE)
- DO NOT modify `opencode-key-router.mjs` directly.
- DO NOT restart the key-router (main-lane coordinates that).
- DO NOT reformat any other files in the repo.
- AFTER writing, run `node --check C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/welfare-providers-ADD-patch.diff`
  to validate your diff format is parseable (or paste the patch contents + verify
  they could be `git apply`-ed). If `node --check` fails (it will — diff files
  aren't JS), fall back to verifying by writing the patched ENTRY SNIPPETS to a
  separate file `tmp/welfare-providers-sanity-check.mjs` and running
  `node --check` on THAT.
- Report back the file lengths + a one-line summary per provider.

# MODEL

Default to whatever lane the launching MCP pins you to (modelscope/zai-org/GLM-5.2
proven today). Reasoning max — HARDCODED default for subagents.

# TIMEOUT

30 min (1800s) standard.

# COST BUDGET

$0 expected. Immediately stop if the route becomes a paid burned route (the
modelscope/zai-org/GLM-5.2 path is FREE per the user-harness setup — should be
$0).

# POST-RUN HUMAN-CHECKABLE ARTIFACTS

- `tmp/s7-dispatch/welfare-providers-ADD-patch.diff` (~3-5KB)
- `tmp/s7-dispatch/welfare-providers-signup-guide.md` (~5-10KB)
- briefly: per-provider summary line + any provider detected as signup-broken today

Main-lane will:
- review patch + apply via `git apply` or hand-merge via `edit` tool
- restart key-router (single coordinated restart)
- dispatch smoke workers per new route once user's API keys are set
- append bench-log row
