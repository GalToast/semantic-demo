--- a/src/opencode-key-router.mjs
+++ b/src/opencode-key-router.mjs
@@ -80,6 +80,35 @@ const isNvidiaToken = /^(nvapi-[A-Za-z0-9]{40,80})$/;
 const isMistralToken = /^(mistral-[A-Za-z0-9]{40,80})$/;
 const isModelScopeToken = /^(sk-[A-Za-z0-9]{32,80})$/;
 const isKiloToken = /^(sk-[A-Za-z0-9]{32,80})$/;
+const isGroqToken = /^(gsk_[0-9A-Za-z]{40,80})$/;
+const isDeepSeekToken = /^(sk-[0-9a-z]{32})$/;
+const isSiliconFlowToken = /^(sk-[0-9A-Za-z]{32,80})$/;
+const isTogetherToken = /^([0-9a-f]{40,64})$/;
+const isCerebrasToken = /^(csk-[0-9a-z]{20,40})$/;
+const isCohereToken = /^([A-Za-z0-9]{40,64})$/;
+const isHyperbolicToken = /^(sk-[A-Za-z0-9]{20,40})$/;
+const isNovitaToken = /^(sk-nnt_[A-Za-z0-9]{32,})$/;
+const isAimlApiToken = /^([A-Za-z0-9]{32,80})$/;
+
 const isOpenRouterToken = /^(sk-or-v1-[A-Za-z0-9]{40,80})$/;
 const isFreemodelToken = /^(sk-[A-Za-z0-9]{32,80})$/;
 const isLogfareToken = /^(sk-[A-Za-z0-9]{32,80})$/;
@@ -250,6 +279,95 @@ const PROVIDERS = {
 		nvidia: {
 			label: "NVIDIA NIM",
 			routePrefix: "/nvidia/v1",
+    groq: {
+        label: "Groq",
+        routePrefix: "/groq/v1",
+        upstreamBase: "https://api.groq.com/openai/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "GROQ_API_KEY",
+                tokenRegex: isGroqToken,
+                configFile: "groq-keys.json",
+            }),
+    },
+    deepseek: {
+        label: "DeepSeek",
+        routePrefix: "/deepseek/v1",
+        upstreamBase: "https://api.deepseek.com/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "DEEPSEEK_API_KEY",
+                tokenRegex: isDeepSeekToken,
+                configFile: "deepseek-keys.json",
+            }),
+    },
+    siliconflow: {
+        label: "SiliconFlow",
+        routePrefix: "/siliconflow/v1",
+        upstreamBase: "https://api.siliconflow.cn/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "SILICONFLOW_API_KEY",
+                tokenRegex: isSiliconFlowToken,
+                configFile: "siliconflow-keys.json",
+            }),
+    },
+    together: {
+        label: "Together",
+        routePrefix: "/together/v1",
+        upstreamBase: "https://api.together.xyz/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "TOGETHER_API_KEY",
+                tokenRegex: isTogetherToken,
+                configFile: "together-keys.json",
+            }),
+    },
+    cerebras: {
+        label: "Cerebras",
+        routePrefix: "/cerebras/v1",
+        upstreamBase: "https://api.cerebras.ai/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "CEREBRAS_API_KEY",
+                tokenRegex: isCerebrasToken,
+                configFile: "cerebras-keys.json",
+            }),
+    },
+    cohere: {
+        label: "Cohere",
+        routePrefix: "/cohere/v1",
+        upstreamBase: "https://api.cohere.ai/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "COHERE_API_KEY",
+                tokenRegex: isCohereToken,
+                configFile: "cohere-keys.json",
+            }),
+    },
+    hyperbolic: {
+        label: "Hyperbolic",
+        routePrefix: "/hyperbolic/v1",
+        upstreamBase: "https://api.hyperbolic.xyz/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "HYPERBOLIC_API_KEY",
+                tokenRegex: isHyperbolicToken,
+                configFile: "hyperbolic-keys.json",
+            }),
+    },
+    novita: {
+        label: "Novita",
+        routePrefix: "/novita/v1",
+        upstreamBase: "https://api.novita.ai/v3/openai",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "NOVITA_API_KEY",
+                tokenRegex: isNovitaToken,
+                configFile: "novita-keys.json",
+            }),
+    },
+    aimlapi: {
+        label: "AIMLAPI",
+        routePrefix: "/aimlapi/v1",
+        upstreamBase: "https://api.aimlapi.com/v1",
+        authHeader: "Bearer",
+        getKeys: () =>
+            loadProviderKeys({
+                envBase: "AIMLAPI_API_KEY",
+                tokenRegex: isAimlApiToken,
+                configFile: "aimlapi-keys.json",
+            }),
+    },
 			upstreamBase: "https://integrate.api.nvidia.com/v1",
 			authHeader: "Bearer",
 			getKeys: () =>