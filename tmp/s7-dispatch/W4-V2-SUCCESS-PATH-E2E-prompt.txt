WORKER S7-W4 — V2-SUCCESS-PATH E2E TEST SCRIPT (test runner = main-lane)
==================================================

# MISSION

Write a comprehensive end-to-end test SCRIPT for the V2 failover SUCCESS
path. This script will be RUN by the main-lane AFTER W1 (slotHandle
fix) + W2 (matrix load) patches land and the key-router restart.

The script must:
  1. POST HTTP requests to the live key-router `http://127.0.0.1:8788`
     on routes known to need failover (X-V2-Failover: 1 header set)
  2. Assert: alternative-carrier dispatch delivers an HTTP 200 result
     with `X-Router-Failover-Applied: true` header (not `false`)
  3. Log every test case as a JSONL line in
     `C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/v2-success-path-results.jsonl`

# SCOPE

  Worker WRITES the test script + tests. Worker SHOULD NOT run it (the
  live key-router hasn't gotten the W1 + W2 patches yet — running now
  would produce the same failure shapes that Sprint-6 W4 already saw).

# CWD

  C:/Users/HP/repos/semantic-explorer

# ALLOWED FILES (worker writes only)

  - WRITES: `C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/v2-success-path-e2e.mjs` (test script)
  - WRITES: `C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/v2-success-path-e2e-REPORT.md` (spec doc + expected results)

# PROCEDURE

  1. Define test cases — choose 4-6 routes that should trigger V2 failover:

     Examples (worker should refine / pick better ones based on the loaded
     matrix `tmp/v2-overlay-matrix.json`):
       - `/zydit/v4` + bogus model `nonexistent-XYZ`
         (Site A trigger — no active keys. Alternative = openrouter/
         north-mini-code:free from matrix should serve the V2 dispatch
         → expect 200 + X-Router-Failover-Applied: true)
       - `/openprovider/v1` + bogus model
         (Site B trigger — lastStatus 502)
       - `/zydit/v1` (catalog dishonest — 200 catalog then 404 dispatch)
       - `/neuralwatt/v1` (402 credits — should trigger Site B)
       - `/llm7/v1` (402 credits)
       - `/freemodel/v1` (401)

     Picking routes: prefer routes that have a known-good alternative in
     the 8-entry v2-overlay-matrix.json. Read that file to find candidates.

  2. Write the Node script. It should:
     - Promote keyboard readline → no (this is non-interactive batch test)
     - Iterate test cases
     - For each: `await fetch('http://127.0.0.1:8788/<route>/v1/chat/completions',
       { method: 'POST', headers: { 'Content-Type':'application/json',
       'x-v2-failover': '1' }, body: JSON.stringify({ model: bogusModel,
       messages: [{role:'user', content:'ping'}] }) })`
     - Read response: status, ALL `x-router-*` headers + body first 200 chars
     - Assert: status === 200 AND `x-router-failover-applied === 'true'`
     - Append a JSONL line: `{ testId, route, model, status, statusOk,
       failoverApplied, failoverAppliedOk, bodySnippet, ts }`
     - After all tests, write summary `{ total, passed, failed, durations }`
       to STDOUT and to `tmp/s7-dispatch/v2-success-path-results.jsonl`
     - Exit 0 if all pass, exit 1 if any failed

  3. Write the REPORT spec doc interweaving the test design:
     - Each test case's rationale (why this route should trigger V2 failover)
     - Expected alternative carrier per the loaded matrix
     - Expected response shape (status + headers we expect to see)
     - Mock-test plan (what to do if the live key-router refuses the test
       case — graceful skip vs hard-fail)

# REQUIRED EVIDENCE

  C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/v2-success-path-e2e.mjs
  C:/Users/HP/repos/semantic-explorer/tmp/s7-dispatch/v2-success-path-e2e-REPORT.md

  Spec doc should clarify:
    - `pass` = status === 200 + failoverApplied === 'true' + body non-empty
    - `partial` = status === 200 but failoverApplied === 'false' (V2 not
      triggered — means gate header not honored, or matrix empty)
    - `fail` = status >= 400 (V2 couldn't dispatch an alternative, or threw)

# MODEL

  Default `pi:router-opencode-zen/mimo-v2.5-free` or `router-agnes` lane
  (free, $0). Reasoning max — HARDCODED default for subagents.

# TIMEOUT

  30 min (1800s) standard.

# COST BUDGET

  $0 expected.

# POST-RUN HUMAN-CHECKABLE ARTIFACTS

  - `tmp/s7-dispatch/v2-success-path-e2e.mjs` (~150-300 lines Node, clean
    ESM `import` syntax, no test framework deps — just `node` executing
    fetch + assert + JSONL append)
  - `tmp/s7-dispatch/v2-success-path-e2e-REPORT.md`

Main-lane will:
  - read the spec doc to verify test cases are sensible
  - run `node tmp/s7-dispatch/v2-success-path-e2e.mjs` AFTER restarting
    key-router with W1 + W2 patches
  - append the summary line to tmp/v2-impl-bench-log.md (W5 worker's job)
  - surface rollup to user

# FAIL-MODE: IF PATCHES NOT YET LANDED

  If worker can SEE that the harness src files don't have the patches yet,
  just write the test script targeting the EXPECTED end-state (post-W1/W2
  conditions). The script will be run later. Don't get blocked on this.
