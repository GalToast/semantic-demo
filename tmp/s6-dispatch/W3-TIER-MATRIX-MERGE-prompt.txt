# Sprint-6 W3: TIER-MATRIX-MERGE — Produce v2-overlay-matrix.json from raw tier matrix

You are a TIER-MATRIX-MERGE worker. The TIER-MATRIX-UPDATE worker wrote
`tmp/model-tier-matrix.json` (215KB / 19 entries distributed as
3 T0 + 11 WARM_CADAVER + 4 CONDITIONAL + 1 SEASONAL). The raw structure has
top-level keys `generatedAt`, `snapshot`, `families`, `reverse`, `unknown`,
`_meta`. The v2-failover-overlay.mjs needs a flat array of modelMatrix entries
in this shape:
```
{
  modelId: 'agnes-2.0-flash',
  routeId: 'agnes',
  carrierType: 'auto',
  contextWindowLimit: 128000,
  qualityPerCapability: { vision: 0, toolUse: 1, code: 2, default: 2 },
  streamingSmooth: true,
  toolExecutionReliability: 'HIGH',
  routingTier: 'T0',
  // optional auto-derived:
  canVision: false, canToolUse: true, canCode: true,
  longContext: true, streamingSafe: true,
}
```

Your job: read `tmp/model-tier-matrix.json`, flatten / normalize its entries
into the above shape for the overlay, and WRITE the normalized matrix JSON
to `tmp/v2-overlay-matrix.json`.

## Files

- READ: `C:/Users/HP/repos/semantic-explorer/tmp/model-tier-matrix.json`
- WRITE: `C:/Users/HP/repos/semantic-explorer/tmp/v2-overlay-matrix.json`

## Task breakdown

Use `Node` (write a small Node script via the write tool) to:

1. Parse `tmp/model-tier-matrix.json` to understand the actual nested structure
   (which keys group entries — likely `families` or `snapshot`).
2. Iterate over every distinct (modelId, routeId) entry discovered.
3. For each, build a normalized entry with the schema above. Map fields:
   - `qualityPerCapability`: from matrix `qualityScores` if present, else
     default `{vision:0, toolUse:1, code:2, default:2}` (T0 strong defaults).
   - `routingTier`: 'T0' / 'WARM_CADAVER' / 'CONDITIONAL' / 'SEASONAL'
   - `toolExecutionReliability`: derive from routingTier
     (T0 → 'HIGH', CONDITIONAL → 'MEDIUM', WARM_CADAVER → 'LOW',
     SEASONAL → 'MEDIUM' / 'UNKNOWN')
   - `contextWindowLimit`: 128000 default (override from matrix if present)
   - `streamingSmooth`: true default (override if matrix says otherwise)
   - Auto-derived fields: `canVision` from qualityPerCapability.vision > 0,
     `canToolUse` from qualityPerCapability.toolUse > 0, `canCode` from
     `.code > 0`, `longContext` from contextWindowLimit >= 32000,
     `streamingSafe` from streamingSmooth.
4. Drop dead routes (zydit*, neuralwatt, llm7, openprovider, freemodel,
   gemini, mistral, cloudflare-kimi-k2.6, modelscope, zenmux — i.e.
   WARM_CADAVER entries). Keep only T0 + CONDITIONAL + SEASONAL = 3+4+1 = 8
   entries in the final overlay matrix.
5. Write the result to `tmp/v2-overlay-matrix.json` as a flat JSON array.

## Verification

- File `tmp/v2-overlay-matrix.json` exists, JSON-valid, has 8 entries.
- Each entry has all required fields per the schema above.
- No WARM_CADAVER entries survived.
- node -e "console.log(require('./tmp/v2-overlay-matrix.json').length)" → 8

## Output

Write your report to:
`C:/Users/HP/repos/semantic-explorer/tmp/s6-dispatch/matrix-merge-report.md`

Format:
- File written: tmp/v2-overlay-matrix.json
- Entry count: 8 (3 T0 + 4 CONDITIONAL + 1 SEASONAL)
- All T0 entries listed
- All CONDITIONAL entries listed with their conditional-fail-mode
- All SEASONAL entries listed (just logfare-kimi-k2.6 with seasonal note)
- Verification checks pass: JSON validity / entry count / no WARM_CADAVER

## Constraints

- DO NOT edit `opencode-key-router.mjs` or any source code.
- ONLY write 2 new files: `tmp/v2-overlay-matrix.json` + Node script.
- All artifacts under `tmp/s6-dispatch/` or `tmp/`.

End with "TIER-MATRIX-MERGE WORKER — FINAL REPORT" summary:
- entries flattened: N
- entries kept: N
- entries dropped (WARM_CADAVER): 11 (per spec)
- Time taken / Cost: agnes-2.0-flash = $0
