You are completing **Ticket A2-6: Add H1 heading**. A MEDIUM-severity a11y fix for heading hierarchy.

## Goal

Add a visually hidden H1 heading to establish proper page structure for screen readers.

## Working directory

`C:\Users\HP\Desktop\Temp while my comp is at the shop\semantic-explorer`

## Background

The a11y audit (2026-06-14) found:
- **A2-6 (MEDIUM):** The heading hierarchy starts at H3. No H1 exists. Screen readers use headings for page structure navigation.

**Current headings:**
```
H3 "Categories" (legend — visible)
H3 "Business Details" (info panel — visible)
H4 "Status", "Contact", "City" (info panel — hidden in idle)
```

**WCAG:** 1.3.1 Info and Relationships (Level A)

## Specific steps

### Step 1: Read the Header component

Read `src/components/Header.svelte`. The existing "SE Semantic Explorer" text at the top could be wrapped in an H1.

### Step 2: Add H1

Add a visually hidden H1. Two options:
1. **Wrap existing header text** — Change the "Semantic Explorer" text in the header to be an H1 with `class="sr-only"` (or equivalent)
2. **Add new H1** — Add `<h1 class="sr-only">Semantic Explorer</h1>` at the top of the main content

**Recommended:** Option 1 — repurpose the existing header text. The "SE" logo + "Semantic Explorer" text already serves as the page title; wrapping it in H1 makes it semantically correct.

**CSS for visually hidden:** The `sr-only` pattern already exists in `css/base.css`:
```css
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
```

If the H1 should be visible (as the app title), skip the sr-only class and just use a properly styled H1.

### Step 3: Verify

- `npm run check` → 0 errors
- `svelte-check` → 0 errors in `src/`
- `npm run test:unit` → all tests pass

### Step 4: Verify with Playwright

1. Navigate to `http://localhost:5173/?nodemo=1`
2. Take a snapshot — verify an `h1` heading appears in the accessibility tree
3. Check that the heading text is "Semantic Explorer" (or similar)

### Step 5: Commit + push

Suggested commit message:
```
fix(a11y): add H1 heading for screen reader navigation (A2-6)

Per a11y audit 2026-06-14, the heading hierarchy started at H3 with
no H1. Now the page title "Semantic Explorer" is an H1 heading,
giving screen readers a proper document outline.

Verified: svelte-check 0, tests pass, Playwright confirms H1 in tree.
```

Push to `origin master`.

## Out of scope

- The 7 off-limits state writers
- The engine kernel (js/modules/*)
- Other components

## File scope

You CAN edit:
- `src/components/Header.svelte` (primary — wrap title in H1)

## Tool surface check

Report exposed tools at start.

## Reporting

When done, return:
1. The commit SHA
2. Verification gates
3. Playwright evidence (H1 in accessibility tree)
4. Files changed

If you hit a blocker, return BLOCKED: with details.
