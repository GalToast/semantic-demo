You are completing **Ticket A2-1 + A2-2: Broken skip link target + Missing `<main>` landmark**. Two HIGH-severity a11y issues with a single combined fix.

## Goal

Add a `<main id="main-content">` wrapper to `App.svelte` so the skip link has a valid target and screen readers gain a `main` landmark for quick navigation.

## Working directory

`C:\Users\HP\Desktop\Temp while my comp is at the shop\semantic-explorer`

## Background

The a11y audit (2026-06-14) found:
1. **A2-1 (HIGH):** The skip link `<a href="#main-content">` in `src/index.html:89` targets `id="main-content"` which doesn't exist in the Svelte app. After activating the skip link, focus stays on the `<a>` element — the user gets no benefit.
2. **A2-2 (HIGH):** The document has no `<main>` element. Screen reader users cannot jump to primary content using landmark navigation.

**WCAG:** 2.4.1 Bypass Blocks (Level A), 1.3.1 Info and Relationships (Level A)

**Evidence:**
```
document.getElementById('main-content') → null
After skip link Enter: activeElement = <A> (skip link itself)
No <main> element in DOM tree
```

## Specific steps

### Step 1: Read the current App.svelte structure

Read `src/App.svelte` in full. Identify the top-level `<div id="semantic-explorer">` and its children. The `<main>` wrapper should go INSIDE this div, wrapping all the visible UI layers.

### Step 2: Add `<main id="main-content">` wrapper

Add a `<main>` element inside the `#semantic-explorer` div. The `<main>` should wrap:
- Canvas (Layer 0)
- SemanticOverlay (Layer 30)
- MapView (conditional)
- Legend (Layer 50)
- WeatherWidget (Layer 50)
- InfoPanel (Layer 80)
- SearchBar (Layer 100, conditional)
- Header (conditional)
- FocusCard, JourneyChrome, FocusPocket (inside #focus-stage)
- MapSummary
- FocusPocketA11y
- CompassRail
- Controls
- Filters
- ThreadInspector
- DemoChoreography
- DevGui, SpectorInspector (conditional)

The `<main>` should NOT wrap:
- LoadingOverlay (Layer 3000 — it's a full-screen overlay, not content)
- Toast (Layer 1200 — it's a notification)
- The hover tooltip, summary card, search trail cue (they're utility elements)

**Approach:** The simplest correct approach is to wrap everything inside `#semantic-explorer` EXCEPT the loading overlay. The `<main>` element should be placed right after the opening `<div id="semantic-explorer">` tag and close before the `LoadingOverlay`.

**Important:** The `<main>` element must be unique per page (only one `<main>` allowed). Don't add `role="main"` — use the native `<main>` element.

### Step 3: Verify

All MUST pass clean:
- `npm run check` → 0 errors
- `svelte-check` → 0 errors in `src/`
- `npm run test:unit` → all tests pass
- `npm run test:contract` → all tests pass

If any fails, STOP and report. Do not commit broken state.

### Step 4: Verify with Playwright

Use Playwright MCP to verify:
1. Navigate to `http://localhost:5173/?nodemo=1`
2. Tab to skip link, press Enter
3. Verify `document.activeElement` is NOT the skip link (should be the first focusable element inside `<main>`)
4. Verify `document.querySelector('main')` exists and has `id="main-content"`
5. Take a snapshot to confirm the `main` landmark appears in the accessibility tree

### Step 5: Commit + push

Suggested commit message:
```
fix(a11y): add <main> landmark with skip link target (A2-1 + A2-2)

Per a11y audit 2026-06-14, the skip link pointed to #main-content which
didn't exist, and the document had no <main> landmark.

Now:
- Primary UI content is wrapped in <main id="main-content">
- Skip link activates correctly (focus moves past the link)
- Screen readers gain landmark navigation to primary content

Verified: svelte-check 0 errors, unit tests pass, Playwright confirms
skip link targets <main> element.
```

Push to `origin master`.

## Out of scope (DO NOT TOUCH)

- The 7 off-limits state writers (per AGENTS.md)
- The engine kernel (js/modules/*)
- The LoadingOverlay or Toast components
- CSS changes (this is a structural HTML change only)
- The skip link styling in `css/base.css` or `src/index.html` (already correct)

## File scope

You CAN edit:
- `src/App.svelte` (primary — add `<main>` wrapper)

You CAN add:
- Nothing new (this is a structural change only)

## Tool surface check

Report exposed tools at start. If Read/Write/Edit/Bash/Glob/Grep are missing, stop and report.

## Reporting

When done, return:
1. The commit SHA
2. Verification gates pass/fail
3. Playwright evidence (skip link target check)
4. The exact files changed
5. Any surprises

If you hit a blocker, return BLOCKED: with details. Do not commit broken state.
