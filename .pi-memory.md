
§
Dual-state-mirror refactor — Phase 1+2 DONE (2026-06-25).

Phase 1 (commit ecfc56bb): Replaced LoosePoint/LoosePoint3D/LooseNeighborCandidate in src/lib/orchestration/adapters.ts with exported Point3D interface (x?, y?, z?, [key:string]:unknown — distinct from webgl's stricter Point3D) and NeighborCandidate interface (8 explicit fields + index signature: index, reason, threadType, source, relationshipRole, roleReason, sameCity, sameStatus). adapter-deps.ts kept `as unknown as BusinessRecord` cast as the bridge to satisfy the dead `_point`/`_focusPoint` params typed BusinessRecord | null.

Phase 2 (commits a4dcd6d2 + 71a87781): Dropped unused `_point`/`_focusPoint` params from summarizeNeighborReason and getInsideRelationshipLabel in src/lib/journey/thread-settler.ts. Both now `(candidate) => string`. 9 files, +33/-101. thread-inspector-adapter.ts lost local Point3D/NeighborCandidate duplicates (imports from adapters.ts). journey.ts:176 kept single `as` cast (sufficient for 1-arg target). 6 caller files updated: canvas-interaction.ts, canvas-hit-test.ts, focus-ui.ts, semantic-dive.ts, thread-inspector-state.ts, journey.ts. 7 cascading dead-local removals + unused BusinessRecord imports cleaned.

Commits (all 0 tsc/lint): 5db0cec3 (split thread-inspector barrel), ae4770aa (eliminate barrel), ce826772 (tsc batch 171→0), ecffd865 (re-aim contract tests), ecfc56bb (drop Loose*), a4dcd6d2 (drop dead params), 71a87781 (update callers).

Subagent pattern: kilo owl alpha via MCP external_subagents, owner_tag kimi-main, mcp_profile:subagent, mode:yolo, timeout:900s. 3 subagents, all 9/10 grade, 0 rate-limit fallbacks.

Worker reports: tmp/thread-inspector-contract-{research,fix}-2026-06-25/*.md, tmp/thread-settler-dead-param-cleanup-2026-06-25/implementation-report.md.

Tests: contract 11/11. Full test:unit 1770/1771 → 1769/1771 (2 RAF tests in three-engine-animate-regression broke from PARALLEL session's three-engine refactor, not ours). <!-- created=2026-06-25, last=2026-06-25 -->
§
Dual-state-mirror Phase 3+4 QUEUED — DO NOT START before parallel-session quiescence (2026-06-25).

Phase 3 — real dual-state mirrors:
- navStore + journeyStore (Svelte stores, 20 readers) vs appState.navState (Svelte 5 rune state).
- Hot spot: syncSvelteNavFromLegacy — 1 def + 10 calls in src/lib/orchestration/window-actions.ts.
- Decision: DELETE Svelte stores + consolidate on appState runes (PREFERRED), OR formally split read boundaries.
- CRITICAL: preserve readLegacyNavField fallback (reads appState.navState first, falls back to window.__APP_STATE__ / __TEST_STATE__ / __semanticState / window.state). Removing breaks contract tests — see W11-T4 pilot failure 2026-06-14.

Phase 4 — tighten legacyState:
- Currently `appState as unknown as Record<string, unknown>`.
- Target: real EngineInternalState interface for 5 consumers: src/lib/engine/{three-engine,cluster-filter-controller,triggers,renderer-diagnostics}.ts + src/lib/orchestration/window-actions.ts.

Why stopped after Phase 2 — 3 parallel sessions active, none committed:
1. three-engine.ts being split into three-engine-{core,mycelium,search}.ts (3 untracked).
2. src/main.ts modified.
3. tests/widget-journey.spec.js modified.

Worth investigating: parallel session re-added `as unknown as` double-cast in journey.ts:176 — Phase 2 worker decided single `as` was sufficient. Either intentional or accidental.

Pre-existing test failures (NOT ours): cursor-surface-preservation-regression (env teardown); three-engine-animate-regression x 2 (test does readFileSync + grep for `export function animate()` — fails because parallel session moved animate() out).

Phase 3 reference files: src/lib/state/app.svelte.ts (source of truth), src/lib/orchestration/window-actions.ts (syncSvelteNavFromLegacy + 10 calls), src/lib/orchestration/navigation-state.ts (readLegacyNavField canonical).

Before Phase 3 dispatch: wait for parallel-session commits, check journey.ts:176 cast re-add intent, inventory 20 readers. <!-- created=2026-06-25, last=2026-06-25 -->
§
Phase 3a (navStore mirror) regression + amend-recovery lessons (2026-06-25).

LESSON 1 — Don't `git commit --amend` when parallel sessions may have committed since your last commit. I committed 00dba4ef, then parallel session's 191ffe4a and d1b28342 landed. I tried `git commit --amend` to fix a regression, but HEAD was the parallel session's commit, not mine. My amend silently rewrote their commit, splicing my navigation.svelte.ts fix into it. Recovered via `git reset --soft d1b28342` then fresh commit. Master ended clean (parallel session's commit preserved + my fix as 49088313).

Rule: BEFORE `git commit --amend`, always run `git log -1 --oneline` to verify HEAD is YOUR last commit. If not, use `git commit` (new commit) instead.

LESSON 2 — Run test:unit AT LEAST TWICE for non-trivial changes. Initial verification was false positive: 1770/1771 first run, 1762/1771 second run after parallel commit landed.

Rule: For any refactor touching shared state (stores, runes), run test:unit twice with parallel-session check between them.

LESSON 3 — state-class-migration-5-navigation tests mock appState with read-only navState getter. Any code writing to appState.navState throws `TypeError: Cannot set property navState of #<Object> which has only a getter`. Future Phase 3b/c work that wants navStore to push to appState.navState must first update this mock.

Master now: 49088313 (my fix) + d1b28342 (parallel session, preserved) + 00dba4ef (original Phase 3a, now with follow-up fix) + 191ffe4a (parallel) + earlier commits.

Final verification: tsc=0 in scope, lint=0, contract 11/11, test:unit 1771/1771 ALL PASS (better than baseline). <!-- created=2026-06-26, last=2026-06-26 -->
§
Dual-state-mirror Phase 3 architectural finding (2026-06-25): navStore and appState.navState are INTENTIONALLY separate sources of truth. They CANNOT be merged.

navStore = runtime source for components (Svelte 5 writable, sync, reactive). appState.navState = global/legacy source for window.__APP_STATE__ compat. W11-T4 readLegacyNavField contract requires appState to be PARTIAL with a fallback chain (window.__APP_STATE__ / __TEST_STATE__ / __semanticState / window.state).

The contract tests (state-class-migration-5-navigation.test.ts) do: set appState.mode = search, call navStore.set with mode='', expect currentMode() returns search (via fallback). If navStore.set pushes to appState, the fallback never fires.

Phase 3a (mirror via $effect.root() for external writers) is the right boundary. Phase 3b/c/d as originally planned (delete navStore, migrate readers) is NOT viable without breaking the W11-T4 contract.

Lesson: read CONTRACT tests for invariants BEFORE refactoring. State-class-migration-5-navigation.test.ts is the contract test that documents the dual-state invariant.

Lessons captured (2026-06-25): (1) Always run test:unit TWICE with git status between — initial verification can be false positive from test isolation. (2) Read contract tests for invariant before refactoring. (3) Subagents benefit from explicit DRIFT REPORTING — they may discover architectural issues that need user-level input. <!-- created=2026-06-26, last=2026-06-26 -->
