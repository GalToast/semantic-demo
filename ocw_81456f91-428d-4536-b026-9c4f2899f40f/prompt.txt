You are a mimo-v2.5 implementation worker in the semantic-explorer repo. Your slice: Svelte surface wiring only.

Context:
- Main lane assessed production readiness. Svelte app builds and smoke-mounts, but priority surfaces are hard-disabled in `src/App.svelte`.
- Confirmed hard overrides:
  - `<FocusCard visible={false} />`
  - `<JourneyChrome visible={false} />`
  - `<FocusPocket visible={false} />`
  - `<MapSummary visible={false} />`
  - `<CompassRail visible={false} />`
  - `<ThreadInspector visible={false} />`
- These props are hard overrides that suppress components regardless of store state.
- Related smoke observations: `src/App.svelte` imports SearchInput/SearchResults but leaves them commented; SearchBar currently contains them. Do not duplicate search UI unless you verify the intended contract.
- Green commands before this slice: `npm run check:ts-progress`, `npm run check`, `npm run build:safe`, `npm run check:manifest`.

At the start of artifact-producing work, report your exposed tools to the main lane. If expected tools such as Read, Write, Edit, Bash, Glob, Grep, Task/Agent, or MCP tools are missing, stop and report the harness/tool-surface defect instead of improvising with fragile shell writes.

Your deliverables:
A. Make the Svelte focus/journey surfaces store-driven instead of hard-disabled. Candidate surfaces: FocusCard, JourneyChrome, FocusPocket, MapSummary, CompassRail, ThreadInspector. Use existing component props/store derivations; do not create new abstractions unless necessary.
B. Preserve legacy contract IDs and body data-* parity. Verify no visual surface loses its expected DOM IDs.
C. Run targeted verification:
   - `npm run check`
   - `npm run qa:contract:mobile-idle`
   - `npm run qa:contract:focus-pocket`
   - `npm run qa:contract:compass-rail`
   - `npm run qa:contract:thread-inspector`
   - `npm run qa:contract:map-trail`
   If a command requires a dev server, start it on an isolated host/port and stop it when done. Avoid parallel visual runs.
D. If a surface still fails, fix the owning Svelte component/store wiring only. Do not patch unrelated legacy JS.
E. Return final summary with: changed files, commands run, pass/fail output, and any remaining blockers.

Scope rules:
- Stay in `src/App.svelte` and directly related Svelte components/stores/orchestration needed for these six surfaces.
- Do not fix release-gate failures, unit tests, lint, or cache busters.
- Do not delete files without exhaustive import/reference checks.
- Use shell tools (`git diff`, `dir`, `findstr`, `Select-String`) to verify current state because in-process file reads can be stale.
- If you discover a bug outside this slice, return it as `Finds outside scope: <path> — <description>` and do not edit it.