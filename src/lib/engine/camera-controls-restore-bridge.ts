/**
 * @lib/engine/camera-controls-restore-bridge.ts - Bridge adapter for camera controls restore.
 *
 * Keep direct legacy imports behind the engine boundary while Svelte-track
 * consumers migrate.
 */

export { noteSceneInteraction } from '../../../js/modules/camera-controls-restore';
