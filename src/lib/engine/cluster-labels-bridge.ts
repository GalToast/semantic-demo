/**
 * @lib/engine/cluster-labels-bridge.ts — Sanctioned passthrough for cluster-labels module.
 *
 * Tracks the legacy cluster-labels module consumption from the render loop's
 * _ensureModules(). Native port is deferred to W11-T10 Wave 3.
 */
export * from '@lib/ui/cluster-labels'
