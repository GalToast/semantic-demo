/**
 * @lib/engine/dom-formatters-bridge.ts — Bridge to legacy DOM text formatting utilities.
 */

export { sanitizePublicFacingNote, cleanPublicNoteText, formatBusinessName } from '../utils/dom-formatters'
