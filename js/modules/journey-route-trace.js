/**
 * journey-route-trace.js — JS re-export stub (pilot retired shadow).
 * Canonical source: journey-route-trace.ts
 *
 * This file preserves backward-compatible .js import paths for the legacy
 * runtime bundle. esbuild resolves the .ts re-export at bundle time.
 */
export {
    resetRouteTraceDiagnostics,
    removeRouteTraceOverlay,
    setRouteChoreographyPhase,
    initRouteTraceSubscriptions,
    updateRouteTraceOverlayPositions,
    refreshRouteTraceOverlay
} from './journey-route-trace.ts';
