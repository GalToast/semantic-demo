/**
 * journey-canvas-interaction.js — JS re-export stub (pilot retired shadow).
 * Canonical source: journey-canvas-interaction.ts
 *
 * This file preserves backward-compatible .js import paths for the legacy
 * runtime bundle. esbuild resolves the .ts re-export at bundle time.
 */
export {
    initJourneyCanvasInteractionAdapter,
    isThreadCandidateVisibleOnCanvas,
    ensureCanvasNodeInteractionBindings,
    removeCanvasNodeInteractionBindings
} from './journey-canvas-interaction.ts';
