/**
 * seeded-random.ts — thin re-export shim
 *
 * Canonical source moved to @lib/utils/seeded-random.ts (Wave 11 T1a).
 * This shim preserves backward compatibility for js/ importers.
 */
export { seededUnit } from '@lib/utils/seeded-random';
