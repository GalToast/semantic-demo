/**
 * journey-semantic-overlay.js — JS re-export stub (pilot retired shadow).
 * Canonical source: journey-semantic-overlay.ts
 *
 * This file preserves backward-compatible .js import paths for the legacy
 * runtime bundle. esbuild resolves the .ts re-export at bundle time.
 */
export {
    resetFocusThreadDiagnostics,
    removeFocusSemanticOverlay,
    refreshFocusSemanticOverlay,
    updateFocusSemanticOverlayPositions,
    getSemanticFocusCueProbeSnapshot
} from './journey-semantic-overlay.ts';
