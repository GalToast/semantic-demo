You are working in C:\Users\HP\Desktop\Temp while my comp is at the shop\semantic-explorer. Task: fix the stale contract tests that fail because they read deleted js/modules/*.js files, starting with the smoke group.

Important constraints:
1. Before artifact-producing work, report the tools you can see in your harness. If expected tools such as Read, Write, Edit, Bash, Glob, Grep, Task, or Agent are missing, stop and report that harness/tool-surface defect instead of improvising fragile shell writes.
2. Do NOT restore deleted JS files. The active runtime source is TS when JS was retired.
3. Keep edits scoped to tests needed for the smoke group and any small helper test utility.
4. Prefer a source resolver helper like tests/source-path.mjs that maps 'js/modules/foo.js' to 'js/modules/foo.ts' when the JS file is absent.
5. For source-only contracts, update them to read the active TS source. For runtime import contracts, do not just change .js to .ts unless you verify Node can import it; if needed, use an esbuild temp-loader helper or convert carefully.

Target failing smoke files from the last run:
- tests/weather-lifecycle-contract.mjs
- tests/weather-surface-ownership-contract.mjs
- tests/camera-auto-rotate-settle-contract.mjs
- tests/scene-reveal-contract.mjs
- tests/motion-state-contract.mjs
- tests/map-flattening-raw-buffer-contract.mjs
- tests/loading-ui-contract.mjs already passed; leave it unless needed for the helper.

Required work:
- Inspect the failing tests and current js/modules TS replacements.
- Add the minimal helper needed so source-only contracts can read TS replacements.
- Update only the smoke contracts that need it.
- Run `npm run test:contract:smoke`.
- If smoke still fails, diagnose whether failures are product regressions or remaining stale paths.
- Run at least one focused build/typecheck if your edits touch source-reading helpers used by other tests.
- Return: changed files, commands run with pass/fail, remaining failures if any, and a concise explanation of why the fix is safe.

Do not touch unrelated app/runtime files unless a test helper requires it.